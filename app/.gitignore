# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
next-env.d.ts.timestamp
*.tsbuildinfo

# Build output
dist/
build/

# Environment — Twelve-Factor III: zero secrets in the repo.
.env
.env.local
.env.*.local
!.env.example

# Test / tooling artifacts
coverage/
playwright-report/
test-results/
blob-report/
.playwright/

# Corpus build artifacts (regenerated by `corpus:build`; the SOURCE records are
# version-controlled, the compiled bundle is not — ADR-003).
corpus/dist/

# Generated PDFs and local uploads (Twelve-Factor VI: no local file writes that
# outlive a request).
tmp/
*.pdf

# Editors and OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*.log
