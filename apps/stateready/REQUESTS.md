# Requests to other modules and fleets

One row per request. **Append; do not rewrite another agent's row.** A request
here is a thing this module could not do inside its own directories, with the
workaround that shipped instead, so nobody has to guess what to swap out.

---

## From B4 — M15, the landing page and the no-login demo (2026-09-04)

| # | to | request | why | what shipped instead |
|---|---|---|---|---|
| B4-1 | login / auth (M9) | **Emit `lp_trial_start`** when a magic-link signup completes, with `placement` taken from the `?placement=` the CTA sets (`/login?from=lp&placement=hero\|proof\|pricing`) | `LANDING_SPEC.md` §10's second decisive ratio is `lp_demo_query → lp_cta_click → lp_trial_start`. This module owns the first two and cannot see the third: the signup completes on a route it does not own | The CTA already carries `from=lp` and `placement`; the event name is exported as `LANDING_EVENTS.trialStart` so the login route can import it rather than retype it |
| B4-2 | billing (M9) | **Emit `lp_checkout_start` / `lp_checkout_complete`**, and **replace the Enterprise `mailto:` with the real `POST /enterprise-enquiry`** route when it lands | `specs/09` gives the enquiry a route, a two-business-day promise and an `enterprise_enquiry_created` event. The landing page publishes the row (D4: the twelve largest accounts on our own prospect list need somewhere to go) but cannot post to a route that does not exist yet | The Enterprise row is a `mailto:` to `SUPPORT_EMAIL` with the subject prefilled, and the click fires `lp_enterprise_enquiry` client-side. Swapping the `href` is a one-line change in `src/components/marketing/landing.tsx` |
| B4-3 | legal (M12) | **`/legal/refunds` must exist and carry both guarantees verbatim.** The landing page's compressed Accuracy Guarantee links to it, and `specs/12` AC8c makes that link one of the four conditions the compression is allowed under | A compression whose "Full terms" link 404s is a compression with no full terms | The link is rendered and tested; if the route is missing the page still renders, but AC8c is only half true until it exists |
| B4-4 | legal (M12) + the pack (M8) | **Put the two canonical guarantee texts in one shared module** that every surface imports — the landing strip, `/legal/refunds`, the purchase screen and the pack's first page | Three surfaces carry the Entry Pack Guarantee verbatim and a fourth carries a compression. Byte equality across four copies is a test, not a design | `GUARANTEES.entryPack` in `src/components/marketing/copy.ts` is asserted **byte-identical to `OFFER.md` §5.1 item 2 read from the document itself** (`tests/landing.test.ts`), so the landing copy cannot drift even before the shared module exists. Move it, and point this module's import at it |
| B4-5 | the pack (M8) | **Swap the sample Entry Pack preview to the real renderer** when `src/lib/packs/**` lands | `LANDING_SPEC.md` §7 calls the redacted sample the single most persuasive artefact we own: it lets a buyer audit the deliverable before purchase. Two renderers of the same document will diverge, and the marketing one will be the one nobody updates | The preview is generated from the committed record by this module's own `buildRulebook`, rendered on `PaperSurface`, and shows the same rows and the same gaps panel the pack promises |
| B4-6 | the `(marketing)` layout owner | **Let a marketing page suppress the layout's footer.** `/` renders the two-row footer `LANDING_SPEC.md` §7 specifies (product / company / legal, the disclaimer at full contrast, the entity's postal address) and the layout renders a second, shorter one underneath it | Two footers on the page a cold prospect reads is the kind of detail that says nobody is looking | The landing footer is a `<section id="footer">` inside the page; the layout's own footer is left alone rather than edited, because it is shared with `/pricing`, `/help`, `/legal/*` and `/coverage` |
| B4-7 | knowledge base (`phase-4-revenue/stateready/`) | **`subject_breakdown` for `tx.electrical`** — TDLR's electrician CE is four hours across NEC, Texas law, 16 TAC ch. 73 and NFPA 70E, and the record carries the hours but not the topics | The divergence card (V3) currently proves *8 versus 4*; with the breakdown it proves **8 versus 4 on different subjects**, which is the sentence the page's caption already makes. Today the caption's "on different topics" is carried by the HVAC side alone | The card renders whatever `subject_breakdown` the record has, per trade, and shows nothing where there is none. No topic is invented |
| B4-8 | knowledge base | **An Illinois plumbing record.** IDPH publishes the sharpest statewide wall we have found — every plumber licence in the state renewing on 30 April — and it is the argument the runway (V2) is built on | The runway has to draw its best lane as a state we do **not** cover, labelled as such. That is honest but it is an argument for a competitor | The Illinois lane is drawn dashed, labelled "not covered yet", and cites IDPH with the date we read it. **The hour count is not used anywhere** — IDPH confirms the annual obligation but not the number (`REVIEW.md` Q13) |
| B4-9 | knowledge base | **`tx.hvac` Class B's exam fee, and the bond position for both classes.** They are the gaps the demo names first, on the flagship state × trade | The demo's gaps panel is a proof point, not an apology, but Texas HVAC is the default view and six named gaps is the first thing a prospect reads about our data | The panel names each gap, says we read five TDLR pages looking, and links all five |
