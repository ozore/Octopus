# `apps/wagelens` — requests to the orchestrator and to other agents' files

One row per change an agent needed in a file it does not own. Nothing here has been applied to
another agent's file; each row names the work-around that ships today, so nothing is blocked.

**Written by:** LANDING + WL-11 (sub-wave B, agent B4). **Date:** 2026-09-04.

| # | file / owner | request | why | what ships today |
|---|---|---|---|---|
| **B4-1** | `src/components/lookup-form.tsx` (unassigned, WL-00's) | **Applied, additive:** `<LookupForm>` gained an optional `action` prop, defaulting to `/lookup`. | The landing page's widget must submit to `/` so the result renders in place under the widget, which is the wireframe in `LANDING_SPEC.md` §3. Every existing call site is unchanged. | the prop, defaulted. Please confirm rather than revert — `/lookup` behaviour is byte-identical. |
| **B4-2** | `src/lib/kb/lookup.ts` (frozen, §3) | Move three queries up into the barrel: **`pickExampleWdNumber`** (the determination with the most modifications on record — the one that can draw a timeline), **`divergenceBetween(olderWdId, newerWdId)`** (what actually changed between two modifications), **`lastIngestRun`** (the footer's honest refresh timestamp). | All three are corpus questions, and WL-08's diff and WL-02's card will want the second one. They live in `src/components/landing/demo-data.ts` today only because §3 freezes `lib/kb` and a landing page is not a reason to unfreeze it. | the queries, in the landing module, reading the corpus tables directly. |
| **B4-3** | `src/lib/kb/lookup.ts` → `corpusHealth()` (frozen) | `corpusHealth()` returns `lastRunStatus: null` — a hard-coded null. It should read the newest `kb_ingest_runs` row and return its status and finish time. | `LANDING_SPEC` §10 requires a live data-refresh timestamp wired to the ingestion job, **and requires it to say so when the last run failed**. Every public page wants this line, not only the landing page. | `lastIngestRun()` in `demo-data.ts`, queried by the landing footer alone. |
| **B4-4** | `src/app/layout.tsx` (composition root) | Self-host the two WOFF2 subsets and drop the `fonts.googleapis.com` link. | `LANDING_SPEC` §12 (finding M11, decision D11) requires **no font CDN** on the landing page; `IDENTITY.md` §7.1 mandates the two families **from Google Fonts**. Both documents' actual goals are met by self-hosting: the typographic system survives and the third-party request disappears. It needs font binaries in the repository and one edit to the root layout, which is a fleet decision and not a landing-page one. | the Google Fonts link, as `IDENTITY.md` §7.1 prescribes and as the brief directs. The e2e already blocks every third-party host, and every family has a metric-compatible fallback, so a blocked CDN degrades to a system face. **No other third-party resource exists on the page** — no script, no image, no icon font. |
| **B4-5** | `src/components/shell.tsx` (frozen, §3) | Consider moving the **postal address** and the **non-affiliation sentence** into `PublicShell`'s footer, so every public page carries them and not only `/`. | CAN-SPAM needs the address on anything we send, and the non-affiliation sentence belongs wherever a government document is reproduced — which is `/lookup`, `/wd/:n` and `/` alike. | the landing page renders its own fuller footer (`src/components/landing/footer.tsx`) above the shell's; the shell's is unchanged. |
| **B4-6** | WL-14 (`src/app/(marketing)/watch/**`, the watch form component) | Render the **consented watch capture** beneath the landing page's lookup result: one **unticked** checkbox labelled *"Email me when DOL modifies this determination."* + `Watch it`, ≤ 3 per address, double opt-in. `LANDING_SPEC` §5.4 budgets 9 words for it inside the 450, and the page has the room (it counts 430). | Finding B5: an email address collected on a public page needs a consent record, a confirmation and a one-click unsubscribe. Those mechanics are WL-14's and building a second copy of them on the landing page is exactly how two consent records disagree. | **the capture is not on the landing page.** No address is collected anywhere on `/`. |
| **B4-7** | WL-09 (`src/app/(app)/billing/start/page.tsx`) | Reword the file-header comment that contains the literal `Start free`. | `tests/naming.test.ts` greps `src/` for that string and currently fails on that comment — the same trap `CLAUDE.md` §7 records. The right fix is the comment, never the grep. | nothing; the suite fails on that one assertion until it is reworded. Everything else is green. |
| **B4-8** | WL-06 (certify screen) | Render `<CertifyDisclaimer>` from `src/components/certify-disclaimer.tsx` **above the certify button**, passing the project's pinned `wdNumber` / `modificationNumber` and `corpusStale` from gate G6. | WL-11's acceptance criterion: the disclaimer appears above the button and the certifications are shown in full, not summarised. The component is built and tested; only the screen that mounts it is missing. | the component, with its own tests. Nothing renders it yet. |
| **B4-9** | WL-01 / WL-10 (onboarding, `src/app/(app)/**`) | Call `recordDisclaimerAcknowledgement(db, { orgId, userId })` once at the end of onboarding, and gate the app on `hasAcknowledgedDisclaimer` (`src/lib/disclaimer-acknowledgement.ts`). | WL-11 V3: the standing disclaimer is acknowledged once and recorded **with the content hash of the document shown**, and a material change re-prompts. The hash, the idempotence and the rename-stability are built and tested; the screen belongs in the signed-in app, which this agent does not own. | the module and its tests. `disclaimer_acknowledgements` already exists in `0000_wagelens_init.sql`, so no migration is needed. |
| **B4-10** | orchestrator | `src/styles/landing.css` is a **new** stylesheet imported by `/` alone, rather than an addition to `src/styles/app.css`. | Four agents were editing this app at once and `app.css` is the file all four would have reached for. One landing, one stylesheet, no merge. Every rule is inside `@layer wl.app` and every colour is a `--wl-*` token (asserted in `tests/landing.test.tsx`). | as described. Fold it into `app.css` at merge time if you prefer one file. |
