# Certly — response to the wave-1b review

**Author:** Iteration author (PIPELINE stage 6). **Date:** 2026-09-03.
**Input:** `REVIEW.md` — **15 blocking, 20 major, 12 minor** — plus the Brand Director's final
identity arbitration (`../IDENTITY_ARBITRATION.md`, landed during this iteration).
**Rule followed on every open judgment:** where the review offered options and no founder was
available, I took **the option that reduces founder liability**, applied it, and listed it under
§4 "Founder can override".

## 0. Result in one table

| | blocking | major | minor |
|---|---|---|---|
| **Fixed** | 12 | 17 | 10 |
| **Fixed differently** (the finding is closed, by a different mechanism than the review proposed) | 1 | 2 | 0 |
| **Delegated** (Brand Director / orchestrator — not my files) | 2 | 1 | 1 |
| **Declined** | 0 | 0 | 1 |
| | **15** | **20** | **12** |

*Fixed = B-01, B-02, B-04, B-05, B-06, B-08, B-09, B-10, B-11, B-12, B-13, B-14 · MJ-01, MJ-02,
MJ-03, MJ-04, MJ-06, MJ-09…MJ-20 · MN-01, MN-02, MN-03, MN-04, MN-06, MN-07, MN-08, MN-09, MN-11,
MN-12. Fixed differently = **B-07**, MJ-05, MJ-07. Delegated = **B-03**, **B-15**, MJ-08, MN-10.
Declined = MN-05. MN-02's identity half is the Brand Director's; my half is done.*

**No blocking finding is left open except B-03 and B-15**, both of which are the Brand Director's
(`IDENTITY.md`, `design-system.css`, `identity/samples.html`, `identity/contrast.py` — files I am
forbidden to edit). Both **landed during this iteration**; §3 records the one residual item I still
need from them.

**Files I edited:** `PERSONA.md` (only the corrections the review names), `UX.md`, `BACKLOG.md`,
`specs/00`–`specs/15`, `specs/schema/coi.v1.schema.json`, `KNOWLEDGE_BASE.md`,
`kb-samples/MANIFEST.md`, `THRESHOLDS.md`, `OFFER.md`, `LANDING_SPEC.md`, `product/CLAUDE.md`.
**One new file:** `specs/00-event-vocabulary.md`.
**Files I did not touch, by constraint:** `REVIEW.md`, `IDENTITY.md`, `design-system.css`,
`identity/samples.html`, `identity/contrast.py`, `README.md`, `../PREREQUISITES.md`,
`../../phase-1-ideation/ERRATA.md`. Nothing was committed or pushed.

**Self-checks re-run after the edits:**

```
schema structural check (additionalProperties / required / $ref)   OK
scripts/landing-words.py LANDING_SPEC.md                           413 / 450   exit 0
```

---

## 1. Blocking findings

| id | status | files and sections changed | what was done, and the decision taken |
|---|---|---|---|
| **B-01** | **fixed** | `specs/schema/coi.v1.schema.json` (`form_edition`); `specs/03` §4, §6, §11 A11, §15; `KNOWLEDGE_BASE.md` §A.2, §D.5, §E; `kb-samples/MANIFEST.md` (`certificates/` table, "sources attempted") | `"2025/12"` added to the enum and named **CURRENT** in KB §A.2, with the NY DFS URL, the footer stamp, the new head paragraph and `verified_by: [identity-agent, wave-1b-reviewer]`; 2016/03 demoted to "still in wide circulation". The blank is corpus row **C16** and golden-set fixture **G17** (a blank tests structure, not values), with a new acceptance criterion A11 asserting it never falls through to `unknown`. KB §E's "Editions" row is re-opened and converted from a habit into a **`kb:check` assertion**: any edition quoted in §A.2 that is missing from the committed schema fails the build. **C16 is a manifest row without a file** — deliberately, with the exact `curl` command and a `kb:check` "pending" state, because the edition is load-bearing now and the bytes are one command away. |
| **B-02** | **fixed** | `PERSONA.md` §2.5 (two rows), §2.9.4; `UX.md` §0, §1.1, §3.1, §3.1b; `KNOWLEDGE_BASE.md` §F.5; `specs/04` §4, `specs/05` §2/§5/§7/§8/§12, `specs/06` §2/§3/§7/§10/§11, `specs/12` §3/§4/§9/§10, `specs/15` §4/§9/§13; `OFFER.md` header; `LANDING_SPEC.md` §3, §14.4; `BACKLOG.md` M5/M6 | **"Covered" retired as a status word.** Green state = **"Meets requirements"**, pill `MEETS`, engine value `meets` at vendor level and `met` at requirement level. The CSV column `vendor_status`, the dashboard counters, the report cover, the free report and the landing page all use it. Tokens were vocabulary-neutral (`--c-ok-*`), so the change cost copy, not code. Tests added: `specs/05` §12 asserts no code path emits `covered`; `specs/06` A4b and `specs/12` A2 assert the string appears nowhere in a rendered page or report. **`PERSONA.md`'s underlying finding survives** in the buyer's better word **"current"**, used only about a document. |
| **B-03** | **delegated → landed, one item open** | *(Brand Director: `IDENTITY.md` §6.4, `design-system.css`, `identity/samples.html`, `identity/contrast.py`)*. My side: `specs/05` §2/§2.1, `specs/06` §3, `UX.md` header | The arbitration landed during this iteration with **seven status states**, each with word, glyph, fill pattern and hue, certified by `contrast.py`: `meets` · `expiring` · `asserted_only` ("Claimed, not evidenced", half-filled disc, vertical hatch) · `gap` · `needs_review` · `not_checked` · `no_certificate`. I mapped every engine value onto them in `specs/05` §2.1 and renamed the vendor state `expiring_30d` → **`expiring`** so the names match exactly. **What I still need: `IDENTITY.md` §6.4 has no row for `expired`**, which is a distinct and frequent vendor state (lapsed, not short). Meanwhile the specs render it in the **`gap`** ramp with its **own word, "Expired"** — safe, because `contrast.py` hard-fails a duplicated *word*. If the Brand Director prefers an eighth row with its own glyph, only one mapping cell in `specs/05` §2.1 changes. |
| **B-04** | **fixed** | `UX.md` §2.2 S12, §3.2 *(the `IDENTITY.md` §12.2/§11 half is the Brand Director's)* | The review screen is specified to **the evidence the extractor actually returns**: value · `raw` as printed · page number · **the `source_text` span rendered as a quotation** · **the quote-gate result in words** · confidence meter. Clicking a field **scrolls the document pane to that page and highlights nothing**. The reveal animation shows fields resolving **in the panel**, not boxes landing on the page. **No coordinates were added to the schema** — a model-reported box is exactly the unverifiable provenance claim this product refuses, and the quote gate is *better* evidence because code checks it. `IDENTITY.md` P6's review test still passes. |
| **B-05** | **fixed** | `specs/11` §2 (canonical, with a retired-definitions table); `THRESHOLDS.md` §1; `OFFER.md` §9 → new §9.1; `UX.md` §1.2, §2.2 S19, §7; `LANDING_SPEC.md` §11; `specs/00-event-vocabulary.md` §2.11, §4 | **One definition: `specs/11` §2, event `activated`** — one `comparisons` row against a certificate the org uploaded, out of `needs_review`, emitted once per org by the comparison job, never by the UI. `OFFER.md` §9's four conditions are renamed the **trial health checklist** and kept as the day-7 nudge instrument. `first_status_rendered` is deleted from `UX.md` and registered as retired. `LANDING_SPEC.md` §11's funnel row now reads `signup_started → checkout_completed → activated → trial_converted`. **Decision:** defining activation to require a gap would make a *clean* portfolio a failed activation — a self-inflicted STOP on the metric that can stop the business. |
| **B-06** | **fixed** | `BACKLOG.md` M10 story; `specs/10` §3.1 (new), §6, §10 A14–A16, §13; `specs/01` §2, §3, A7; `specs/11` A10; `LANDING_SPEC.md` §2, §2.1, §6, §9, §14.3; `OFFER.md` §8.2, §9 | M10's story now reads *"with a card and no charge until day 14"*. **Every "Start free" is "Start 14-day trial"**, with *"Card required. No charge until {date}. Cancel in one click."* rendered **adjacent in body text, not behind a link**, `{date}` a real computed date, and the same text in the Checkout line-item description. **Consent is recorded** — a `trialConsents` row storing the exact disclosure string shown, the price, the first-charge date and amount, plus an M9 audit event. **The T−3 and T−1 pre-charge warnings are transactional and cannot be switched off.** "Get a free Gap Report" keeps the word *free*, because it is the only free thing on the page. |
| **B-07** | **fixed differently** | `specs/15` launch gate (new), §2, §4, §5.1 (new), §6, §6.1 (new), §8, §9 A7b–A7d/A13, §11, §13; `LANDING_SPEC.md` §2.1, §8.2, §14.3; `OFFER.md` §4 P1, §5 B5, §13.3 Q2; `BACKLOG.md` M15, N12 | The review asked for the RESEARCH §7 conditions "scaled honestly". I did that **and wrote the reconciliation out condition by condition** in `specs/15` §6.1, because a reduction that is not justified in writing is just a relaxation. **Tightened beyond the review:** no producer `contact_name`/`phone`/`fax`/`email` is **ever stored** on this path (a strip step before persistence, with a test); source files deleted **inside the render job** (minutes, not 24 hours); purge at **7 days**, stated **next to the drop zone in body text**; ≤ 50 MB per session added. **Reduced, and justified:** 25 files not one (a portfolio finding is the whole offer; one file is the demo we already have), images as well as PDF (the buyer photographs paper), written to Blob rather than held in memory (a 20 MB scan cannot pass through a Vercel Function body at all, and the property §7 wanted — *the file does not outlive the job* — is preserved and now **testable**). **The founder's legal read is a launch gate**: until it lands the page ships the samples-only demo as its single hero CTA and the Gap Report behind a waitlist line, and the strict one-file/5 MB/24-hour fallback needs no new code. |
| **B-08** | **fixed** | `specs/03` §4 (`extractions`, CHECK constraint); `specs/15` §5, §9 A7d, §13 | **Option (a) taken**, as the review recommended: `extractions.documentId` and `.orgId` become nullable, `gapReportDocumentId` is added, and a CHECK enforces **exactly one owner**. One extraction table means **one eval pipeline**. Option (b) — a real `organisations` row flagged `is_ephemeral` — was rejected because it pollutes `organisations` and every funnel query in M14, which is the instrument that decides persevere/iterate/stop. The security test is rewritten against the chosen shape: rows with `gap_report_document_id IS NOT NULL` must be unreachable from every org-scoped repository method. |
| **B-09** | **fixed** | `specs/15` §4 (headline), §4.1 (new required section), §9 A12, §13; `specs/12` §3 item 6; `specs/14` §3.2; `THRESHOLDS.md` §6 | The free report now carries a required section — **"Read, but not confident enough to compare (n)"** — naming each document by the file name the visitor gave it, with **the reason in words** taken from the trigger. The headline states **both** counts. New acceptance criterion A12 is the review's exact scenario (18 documents, 6 in `needs_review`). Added beyond the review: the same section in the paid report (`specs/12` §3 item 6), a split of the review-rate panel by surface (`specs/14` §3.2), and a leading indicator in `THRESHOLDS.md` §6 — above ~30% the free report is telling strangers more about our uncertainty than about their portfolio. |
| **B-10** | **fixed** | `specs/10` §2, §2.1 (new), §3, §6, §7, §8, §10, §11, §13, §14; `OFFER.md` §8.1, §8.2, §8.3, §8.4, §12.1, §12.2, §12.3; `LANDING_SPEC.md` §6; `specs/13` help article 12; `BACKLOG.md` §0, M10 | **Decision: keep `specs/10`'s meter, change the name** — the review's ruling. The unit is **tracked vendors**; `cert_limit` → `vendor_limit`, `cert_increment` → `vendor_increment`, **Certificate Pack → Vendor Pack**, "$0.55 per certificate" → "per tracked vendor". **One canonical sentence** now lives in `specs/10` §2.1 and is reproduced **verbatim** in `OFFER.md` §8.1, `LANDING_SPEC.md` §6, help article 12 and `/settings/billing` — and it says plainly that *a vendor who has not sent anything yet still occupies a slot*, because that is the finding the customer is buying. Done **before** the founder creates any Stripe object, which is the whole point of doing it now. |
| **B-11** | **fixed** | `specs/08` header, §2, §5, A10, §11; `specs/07` header, §6, §6.1, A14; `UX.md` §1.2, §2.2 S20, §4.3; `BACKLOG.md` SH-1; `OFFER.md` §4 P2, §2.4 | Every literal domain is now `{APP_ORIGIN}`, `{INBOUND_DOMAIN}` or `{SENDING_DOMAIN}`, read from env, with `specs/08` §2 stating that the launch value is the project's `*.vercel.app` URL (`PLAN.md` D3 ships no custom domain). A test greps the built output for a hardcoded domain and fails on one. The reason is written into the spec so it survives: **`certly.app` is not ours** — `IDENTITY.md` §2.1 records it returning HTTP 200 with someone else's parked "Create Next App" placeholder — and an agent-facing link is the one URL in this product a stranger must trust on sight. |
| **B-12** | **fixed** | `KNOWLEDGE_BASE.md` §F (preamble), §F.4, §F.5; `specs/13` §4, §12; `specs/03` A10; `specs/02` A8; `specs/06` A9; `specs/08` A9; `specs/12` A4/A12; `specs/15` §13 | **KB §F.1/§F.2/§F.3 are the single place a disclaimer text is written**; `src/lib/kb/disclaimers.ts` is generated from them; every surface renders verbatim from that module; no other document may restate, paraphrase or shorten one. `specs/13` §12 now enforces **both halves** — verbatim on all eleven surfaces, **and** a grep that fails the build on a near-duplicate string anywhere else in the repo. The conflicting `IDENTITY.md` §4.4 rule-4 text must become a **pointer**; that edit is the Brand Director's and is named in KB §F's preamble. |
| **B-13** | **fixed** | `LANDING_SPEC.md` §4 §2/§7, §5 V1/V2/V4/V5, §8.1, §9, §14.3; `kb-samples/MANIFEST.md` §Licence 2a (new); `BACKLOG.md` N11 | The demo, the V1 data, **V4 and V5** are rebuilt from **Certly-authored fixtures** — our own layout, fictional vendors, fictional insurers, fictional policy numbers, real ISO form numbers — stored at `apps/certly/src/lib/demo/fixtures/`, never in `kb-samples/`. **V4 is no longer a trace of the ACORD form**: it is our own diagram of a certificate's *anatomy*. The **attributed verbatim quotation** of the ACORD notice stays — it is the strongest asset on the page and it is not a reproduction. The chip relabelling problem disappears with the corpus. **"Authorised" is now defined** in `MANIFEST.md` §Licence 2a: *written permission from the rights-holder, on file, naming the product, the document and the use* — and explicitly **not** "it is on a .gov site", "no login was needed", "it says sample on it" or "there is no copyright notice". Publicly reachable is a fact about a URL; permission is a fact about a rights-holder. |
| **B-14** | **fixed** | **new `specs/00-event-vocabulary.md`**; `specs/14` §3.1, §7 A4b, §11; `THRESHOLDS.md` header, §7; `BACKLOG.md` every analytics column + §5 spec index; `UX.md` §7; `LANDING_SPEC.md` §11 | `specs/14` §3.1 now measures **`trial_converted ÷ activated`**, with `checkout_completed ÷ activated` kept beside it as a **diagnostic** (the gap between them is the trial cancel rate, which retires `H-OF-1`). A single registry file defines every event name with its owning spec and its properties, plus a **retired-names table** so a reader of an older draft knows what happened, and an **`events:check` CI rule** that fails on any name in `THRESHOLDS.md`, `BACKLOG.md`, `LANDING_SPEC.md`, `UX.md` or a spec that does not resolve to a row. The `EventName` union is generated from it, so a typo is a compile error. |
| **B-15** | **delegated → landed** | *(Brand Director: `IDENTITY.md` §6.1–§6.5, §7.1, §14.1, §17.3)*. My side: `UX.md` header, `LANDING_SPEC.md` header token table | `IDENTITY.md` has been regenerated against the arbitrated `design-system.css`. On my side I bound the two documents that named placeholder tokens: `LANDING_SPEC.md`'s `--state-met` / `--state-asserted` / `--state-gap` / `--ink` / `--paper` / `--rule` are replaced by the real `--c-ok-*` / `--c-ast-*` / `--c-gap-*` / `--c-ink` / `--c-paper` / `--c-line`, and both files now name **Source Sans 3 + Source Code Pro on cool office white `#E8EEF6`** and state that Public Sans, IBM Plex Mono and the old warm paper `#F3F3EE` appear nowhere. No literal colour or family name is written outside `design-system.css`. |

---

## 2. Major findings

| id | status | files and sections changed | what was done |
|---|---|---|---|
| **MJ-01** | **fixed** | `specs/03` §15 (rewritten), §15.2; `KNOWLEDGE_BASE.md` §D.5; `THRESHOLDS.md` §4.1; `kb-samples/MANIFEST.md` | Membership lives in **one place** (`specs/03` §15) at **17 real + 4 synthetic = 21**. G3 and the former G8 were the same Durham County file and are merged into one fixture carrying both assertions and **one denominator**. **E1 lives in `endorsements/`, not `certificates/`** — stated, not silently counted. G17 is the new 2025/12 blank. KB §D.5 no longer restates the list; it explains the composition and names each failure a slice prevents. It says plainly that hand-labelling is a **two-day wave-2 task with a named owner and a second reviewer**, and that it is a **gate, not a chore**. |
| **MJ-02** | **fixed** | `specs/03` §15.1 (new); `THRESHOLDS.md` §4.1 | The gate is now **"at most `N_ship` wrong out of `D`"**, with `D` **computed from the expected-value files** — a critical value is in the denominator only if it is printed on that document — and published with its date by the golden-set owner. The old *"16 × 6 ≈ 96"* is named as an estimate presented as arithmetic and its four errors are listed. Until `D` is published the gate is **unrunnable and M4 cannot be declared done**, and both documents say so. |
| **MJ-03** | **fixed** | `BACKLOG.md` M13, SH-7; `specs/13` §2 (Team), A12 | Invitations, the owner/editor/viewer matrix and **server-side seat enforcement against the plan's `seats` metadata** are promoted into M13's scope, where `specs/01` §4 and `specs/13` §2/§7 had already put them. `SH-7` is re-scoped to what is genuinely deferred: role granularity, per-property permissions, bulk member operations and a seat-management UI. A11 asserts the 11th invitation on Standard is refused server-side. |
| **MJ-04** | **fixed** | `LANDING_SPEC.md` §2, §2.1 (new), §3, §4 §1/§2, §9, §11, §14.3 | **One hero CTA.** The demo becomes an **in-page interaction** — the chips already peek above the fold and the hero button scrolls to them. The cited justification is rebutted in place: Poyar's +26% measured a freemium path alongside a card-required trial in a **pricing** context, not two hero buttons. The dual-CTA idea survives as **experiment 3** in §11. The CTA's *label* is gated on B-07's legal read; both states have exactly one button. |
| **MJ-05** | **fixed differently** | `LANDING_SPEC.md` §10 *(the `IDENTITY.md` §7.1/§16.4 half is the Brand Director's)* | The delivery mechanism is now specified where it is enforced: **`next/font`, self-hosted at build time**, which satisfies `IDENTITY.md` §7.1's "exactly one stylesheet link" *and* `LANDING_SPEC.md` §10's "third-party requests: 0 on first view, no CDN fonts" — and the Lighthouse assertion is restated as **failing**, not warning. The `fonts.googleapis.com` link survives only in `identity/samples.html`, a local gallery that is not a shipped surface. I could not edit `IDENTITY.md` §7.1 itself; the substance is now unambiguous in the document CI reads. |
| **MJ-06** | **fixed** | `KNOWLEDGE_BASE.md` §F.4 (new, eleven surfaces); `specs/02` A8, `specs/03` A10, `specs/04` A7, `specs/06` A9, `specs/08` A9, `specs/11` A9, `specs/12` A4/A12, `specs/13` §12/A13, `specs/15` §13; `UX.md` §3.1 | The list goes from five to **eleven**, adding every screen that renders a status and was missing: vendor/party detail, the expiry timeline, the global search result row, the mobile card list, the shared report link `/r/[token]`, and the Free Gap Report's on-screen view. Each has an acceptance criterion in its own spec, and `specs/13` §12's verbatim test now runs over all eleven. |
| **MJ-07** | **fixed differently** | `OFFER.md` §2.2; `KNOWLEDGE_BASE.md` §B.4 | The review offered "mark it as an estimate **or** replace it with a counted number". **I removed it instead**, in both places, and said why: a hedged "about 60%" is still a number on a page, and `BACKLOG.md` N10 bans that shape; counting the corpus by eye today would have produced a *second* unverified number. Both documents now name the number we will actually have — the share of golden-set certificates with `Y` in a tick column and no attached endorsement page, published with its denominator and date once labelling finishes — and `asserted_only_detected` as the live measure. **Until one of those exists, no share is stated anywhere.** Lower liability, and it costs the argument nothing: the mechanism carries it. |
| **MJ-08** | **delegated** | *(none — `../../phase-1-ideation/ERRATA.md` is not mine)* | Flagged for the orchestrator, and recorded in `product/CLAUDE.md` §10.4 so no downstream agent lifts E5's wording. The accurate restatement, for whoever edits it: *the wedge is **reading the document and separating proof from assertion**, not price.* |
| **MJ-09** | **fixed** | `specs/11` §9, A8c; `specs/10` §3 | The phantom **25-vendor trial cap** is deleted. The trial runs at the chosen tier's 50/150/400. The 25 that exists is the Free Gap Report's **document** cap and nothing else — both specs now say so, and A8c asserts no such cap exists in code. |
| **MJ-10** | **fixed** | `specs/10` §8.1 (new matrix), §9, A13, §14; `specs/04` §6; `THRESHOLDS.md` §1 | **Ruling applied: onboarding is free and un-gated up to and including the first comparison** — that comparison *is* activation, it is what the trial is for, and Checkout is offered at the finding screen where the customer has just seen a real gap. A **`no_subscription`** row joins the entitlement matrix with a stated free-onboarding allowance (**25 vendors, 3 documents, unlimited comparisons, reminders off**), deliberately enough to activate and deliberately too small to run a portfolio on. §14's unit test now covers every plan × status **including `no_subscription`**. |
| **MJ-11** | **fixed** | `LANDING_SPEC.md` §4 (all tables normalised), §14.1, §14.2 | §4's tables are uniform `\| Element \| Copy \| Words \|`, and **the counter is published in §14.1** as a runnable script that CI runs. The three "How it works" step labels are counted. The result is **413 / 450**, printed in §14.2 as the script's own output, with per-row counts corrected where the hand-stated numbers were wrong. A pull request that pushes the page over 450 now **fails the build**. |
| **MJ-12** | **fixed** | `PERSONA.md` §9.4 *(the `IDENTITY.md` §17.5 half is the Brand Director's)* | `PERSONA.md` §9.4 is struck through and marked **resolved by `BACKLOG.md` N12 + `OFFER.md` §9**, with the reasoning kept in place so it is not re-litigated: every document costs a real model call, free users upload the messiest documents, and a free tier that fixes conversion by removing revenue has not fixed anything. |
| **MJ-13** | **fixed** | `OFFER.md` header + §12.1/§12.2; `LANDING_SPEC.md` (product-name usage); `specs/07` header; `specs/08` header; `specs/10` §2 | Convention adopted: **the code slug stays `certly`** (`PLAN.md` A3, the phase-3 lists, the Vercel projects) and **every customer-facing occurrence renders `{PRODUCT_NAME}` from one constant**, with a **NAME PENDING** banner at the top of `OFFER.md`, `specs/07` and `specs/08`. The Stripe **product names** in `OFFER.md` §12.1 now read `{PRODUCT_NAME} Starter` etc., with a note that `app=certly` and the 22-character statement descriptor `CERTLY` stay internal. This is the one place that must be settled before the founder creates the objects. |
| **MJ-14** | **fixed** | `specs/08` §6, A5b, §11 | **Resolved in favour of the UX**, as the review recommended: expired and revoked tokens **name the org** (they were real links a real agent holds); an **invalid** token gets a generic page naming nobody. §11's test now asserts identical responses — status, body length and timing bucket — for **invalid vs never-existed**, and the enumeration defence rests on constant-time comparison plus rate limiting. The withdrawn requirement is named as withdrawn rather than quietly deleted. |
| **MJ-15** | **fixed** | `specs/03` §6 | A banner marks the section **abridged and non-normative**, states that the committed file wins and that a disagreement is this section's bug, and both omissions are fixed: `schema_version` is added as a property **and** to `required`, and `endorsement_forms_mentioned` is added to `required`. |
| **MJ-16** | **fixed** | `specs/07` §6, §6.1 (new), §6.2 (new), §7, §8, §9, A5/A9/A13; `BACKLOG.md` N13 (new); `UX.md` §4.2 | The five footer elements are written out: sender identification (`From` = *"{Customer Org} via {PRODUCT_NAME}"* plus the body line), TheVillage's **physical postal address** as sender of record, a conspicuous opt-out with **both** scopes — *"stop requests from {Customer Org}"* **and** *"stop all {PRODUCT_NAME} requests"*, the second being the one that satisfies the statute — non-deceptive subject and headers, and an **absolute ban on marketing** in a vendor-facing email, added to the NEVER list as **N13** with an explicit link allowlist test. `suppressions` gains a `scope: 'org'\|'global'` column. Every V-email carries the full footer even though a document request under an existing business relationship is arguably transactional: four lines, and the argument disappears. |
| **MJ-17** | **fixed** | `specs/03` §9 (upload flow), §10; `specs/08` §5; `specs/15` §7, §6.1 row 4; `BACKLOG.md` §4b | **Client-direct uploads with a server-issued token** (Vercel Blob's client-upload flow) on M4, M8 and M15: the browser asks a route handler for a short-lived, single-use, key-scoped token, PUTs straight to Blob, and the server receives only the reference and **re-reads size and content type from the object itself**, never trusting the client. The 4.5 MB figure is stated with "at the time of writing" and **wave 2 must re-verify the platform limit at build time and record the measured number with its date**. The one path left as a normal POST is the ≤ 5 MB CSV, and that is said explicitly. |
| **MJ-18** | **fixed** | `specs/schema/coi.v1.schema.json` (`limits[]`); `specs/03` §4, §6; `specs/05` §9; `specs/12` §4 | `label_raw` (a `StringField`) added to the limit item in the committed schema and to `coverage_limits` in the Drizzle model, both `required`. The structural check passes. `specs/05` §9 says how it is matched for `OTHER:` rows and `specs/12` §4 exports it as `found_label_raw`, so a Professional-Liability or Cyber limit no longer loses its printed label on the way to the customer's spreadsheet. |
| **MJ-19** | **fixed** | `OFFER.md` §6.1, §6.2 L3; `UX.md` §4.1 C4; `specs/13` §2; `LANDING_SPEC.md` §4 §8 | (a) The **annual remedy is stated on the page and in the guarantee**: "a month's credit on an annual plan" — 1/12 of the annual price, capped exactly as the monthly remedy is. (b) **The expiry warning is made non-optional**, which is the review's own preferred option: everything else stays opt-out, the guarantee stays simple, and a customer can no longer disable the warning the guarantee is conditioned on. The old clause *"did not turn reminders off"* — which referred to **vendor** reminders — is deleted from the promise. |
| **MJ-20** | **fixed** | `kb-samples/MANIFEST.md` §Licence 3; `specs/03` §15.3 (new); `specs/13` §12 | **The blanket assurance is withdrawn**, not softened: the manifest now states that C2 is described in the same file as a genuinely issued certificate, that C11 and C12 are unstamped and may be too, and that the claim is `UNVERIFIED` until checked **by eye** — with a named owner (the golden-set labeller, whose pages are open anyway) and a stated consequence if a real name is found. The rule that matters is now **mechanical**: `evals/redacted-names.json` plus a test asserting none of those strings appears in eval output, prompts, UI copy, help articles or marketing, and `specs/15` §5.1 drops the field entirely on the anonymous path. |

---

## 3. Minor findings

| id | status | change |
|---|---|---|
| **MN-01** | **fixed** | One name: **`gap_report_ready.cost_cents`** in `specs/15` §11/§12, `THRESHOLDS.md` §5 and the registry. |
| **MN-02** | **fixed on my side, delegated for the identity** | `UX.md` §6 item 6's `aria-label` now says *"then **no certificate on record**"* — a statement about the record. `IDENTITY.md` §9.2 carries the same sentence and is the Brand Director's to change. |
| **MN-03** | **fixed** | Covered by B-03: `asserted_only`, `not_checked` and `no_certificate` all have a word, a glyph, a pattern and a hue in the arbitrated `IDENTITY.md` §6.4, and `specs/05` §2.1 maps every engine value onto them. |
| **MN-04** | **fixed** | The requirement-level state is renamed **`undetermined`** in `specs/05` §2/§3/§5/§8/§10/§11; `needs_review` keeps its single meaning as an **extraction** status. The two share the identity's `needs_review` **status state** and its word at the display layer, which is correct — both mean "a human should look" — and the mapping is stated in `specs/05` §2.1 so the scopes cannot blur again. |
| **MN-05** | **declined — not in my edit scope** | `README.md`'s status table is the workspace's own file and is not in the list of files I may edit. Flagged for the orchestrator in `product/CLAUDE.md` §10.4. Every deliverable it lists as "pending" is in fact reviewed and iterated. |
| **MN-06** | **fixed** | `THRESHOLDS.md` §2: **1/0.15 ≈ 6.7 months, ≈ $660** at $99, with the arithmetic error named rather than the rounding excused (7.5 months is 1/0.133, i.e. 86.7% retention). |
| **MN-07** | **fixed** | Harmonised on the **date**, which is better and matches `IDENTITY.md` §15.4: `specs/10` §6's trial banner shows the first-charge date from day 1 and adds days-remaining from day 7; `UX.md` §2.1 S6 follows it. |
| **MN-08** | **fixed** | `PERSONA.md` §9.1 closed: Evident **was** reached, by the Offer & Landing agent, on 2026-09-03, with what it published. The conclusion is unchanged. |
| **MN-09** | **fixed** | `specs/02` §6 now accepts **two kinds** of `acceptsForms` entry — an ISO/NCCI-shaped number, or a carrier proprietary form as free text with a visible **"unrecognised form"** marker. A10 asserts `RSCG0303` is accepted, since `KNOWLEDGE_BASE.md` §C.5 and `specs/05` §4 require the engine to handle it. |
| **MN-10** | **delegated** | `IDENTITY.md` §12.1's drop-zone "always shows the forward-to address" is the Brand Director's line. On my side, `UX.md` §1.2 step 4 now states that forward-by-email is `SH-1`, not MVP, and that the door reads "coming soon" or is hidden at launch. |
| **MN-11** | **fixed** | `LANDING_SPEC.md` §8.1 states it **once**: each fixture is run through the real extractor and the real template **at build time**, and the stored result is what the page serves — **no model call at request time**, with the latency-and-availability reason given. |
| **MN-12** | **fixed** | `specs/06` §3: the six buckets are **mutually exclusive and exhaustive over the non-archived roster** and sum to it; `no_certificate` is a counter like the others and *also* surfaced above the table. A1 is rewritten to a set of six numbers that sums to 80, and a property test asserts the invariant over 1,000 random fixtures. |

---

## 3b. The cross-document decisions (`REVIEW.md` §2 and §3), and where each landed

These are rulings rather than numbered findings, so they are recorded separately.

| ruling | status | where it landed |
|---|---|---|
| **§2.1 Status vocabulary** — "Covered" retired, green = "Meets requirements", engine value `meets` | **applied** | B-02 above; canonicalised in `KNOWLEDGE_BASE.md` §F.5 and `specs/05` §2 |
| **§2.2 The full state set** | **applied, and reconciled with the arbitration** | `specs/05` §2 (five requirement states) and §2.1 (the mapping onto the identity's seven status states); `specs/06` §3 (six vendor states); `specs/03` §8 (six document states). `expiring_30d` renamed **`expiring`** to match the identity exactly |
| **§2.3 Activation** | **applied** | B-05 above |
| **§2.4 The extraction review screen — no bounding boxes** | **applied** | B-04 above |
| **§2.5 Tier metric** | **applied** | B-10 above |
| **§2.6 The Free Gap Report** | **applied, tightened** | B-07 above; `specs/15` §6.1 |
| **§2.7 Trial, limit and lapse behaviour** | **applied**, one item delegated | `specs/10` is canonical throughout. `UX.md` §2.1 S6 corrected (past due = 7 days grace, then read-only — the old *"nothing stops; we keep reading and stop sending"* was wrong and reading is the expensive part). `UX.md` §4.1 C6 corrected — it was **an email to a paying customer making a promise the product will not keep**. Trial CTA and disclosure per B-06. **Delegated:** `IDENTITY.md` §4.3's stale *"$299 to **500**"* must read **400** — Brand Director |
| **§2.8 Reminder ladder** | **applied**, one item delegated | `KNOWLEDGE_BASE.md` §B.5 / `specs/07` §2's T−60/−30/−14/−7/−1/T+1/weekly-to-T+28 is canonical; `UX.md` §4.2 V2's stale "−30/−14/−3" corrected. **The caps the copy promised and nothing implemented are now in `specs/07` §9**: a **72-hour per-recipient minimum interval** across every org and property, and a **per-expiry cap of 6 messages per recipient / 10 in total**, after which the vendor is flagged "we have stopped asking". `specs/07` §6 item 5 makes the composer state *"message {n} of {total}, and they stop as soon as a current certificate arrives"*. **Delegated:** `IDENTITY.md` §12.10's stale ladder — Brand Director |
| **§2.9 "We never charge your vendors" must reach the terms** | **applied** | `specs/13` §4 (Terms) and acceptance criterion A11; cross-referenced from `UX.md` §4.2, `LANDING_SPEC.md` §13 and `PERSONA.md` §9.5. Recommended default **yes, permanent** (OQ-9) |
| **§3 Storage — Vercel Blob behind a `DocumentStore` interface, browser-direct uploads** | **applied, OQ-6 closed** | `specs/03` §9 (the interface and the flow), `specs/04` §4/§6, `specs/08` §5, `specs/12` §6, `specs/15` §2/§5/§7, `BACKLOG.md` §4b, `product/CLAUDE.md` §7 OQ-6. Neon holds `storageKey`, never bytes; the `S3Store` adapter is the written escape hatch at ~500 GB or visible egress |

---

## 4. Founder can override

Nothing here was decided by a founder. Each is the **lower-liability option, applied**, so that silence
ships something defensible. **The orchestrator should paste this table into `PREREQUISITES.md`** — I
cannot edit that file. Numbering follows `REVIEW.md` §5.

| # | question | what ships if nothing is said | how the founder overrides |
|---|---|---|---|
| **OQ-1** | The guarantee | `OFFER.md` §6.1 as written, **with MJ-19's two fixes**: the annual remedy defined as a one-month credit, and the expiry warning made non-optional. Exposure capped at one month | strike it before launch, or take §6.3's service-guarantee fallback |
| **OQ-2** | A $49 Solo tier | **No at launch.** The test is pre-committed in `THRESHOLDS.md` §3 Round 1 and only fires if activation and retention are strong and conversion alone fails | run the test early |
| **OQ-3** | Permanent free tier | **No** — already decided by `BACKLOG.md` N12 + `OFFER.md` §9; `PERSONA.md` §9.4 marked resolved | reopen it against the unit economics |
| **OQ-4** | **Anonymous uploads on the Free Gap Report** | **Yes, under the conditions reconciled in `specs/15` §6.1** — no producer personal data, source files deleted at render, 7-day purge, terms next to the drop zone, daily spend cap — **and only after a legal read. This is a launch gate.** Until it lands the page runs the samples-only demo as its single hero CTA and a waitlist line | the legal read; or take the strict one-file / 5 MB / PDF-only / 24-hour fallback in §6.1 |
| **OQ-5** | ACORD licence | **No licence, because we never render an ACORD form** — and B-13 makes that true by removing the traced form from the landing page. Reading and quoting a printed notice is not reproducing a layout | ask for a real ACORD 25 on the site or a generated ACORD form in-product — that is a licensing conversation with ACORD |
| **OQ-6** | Document storage | **Vercel Blob behind a `DocumentStore` interface, with browser-direct uploads.** Closed in `product/CLAUDE.md` and recorded in `BACKLOG.md` §4b | ask for S3; the adapter is already the escape hatch |
| **OQ-7** | A.M. Best budget | **No budget at launch.** The requirement renders as `not_checked` in the dashboard and in its own report section — honest, and itself a differentiator | buy the data when a GC names it as a lost-deal reason twice |
| **OQ-8** | Inference cost, $0.02 vs $0.10–0.20 | **Plan on $0.10–0.20.** Consequence accepted and written into `OFFER.md` §4/§5: a 25-document free report costs **$2.50–5.00**, so `specs/15` §11's daily spend cap is a launch requirement | nothing — `extraction_succeeded.cost_cents` settles it in week one |
| **OQ-9** | "We never charge your vendors" as permanent | **Yes, permanently, and it is in `/legal/terms`** (`specs/13` §4/§A11) | strike the clause before launch |
| **OQ-10** | Trademark and the rename to Coverfile | **Decide before the Stripe products are created.** Recommended: rename the customer-facing name, keep the slug `certly`. Run a real USPTO class 9/42 search first; spend nothing until it is done | name it |
| **OQ-11** | Davis-Stirling (California HOA) unreachable | **No California-specific HOA template ships**, which is what the KB already does | open the page from a browser and paste the text |
| **OQ-12** | Sending domain, **inbound domain**, **app origin**, CAN-SPAM postal address | Blocks M7 and M8 entirely. `PREREQUISITES.md` P6 and P10 cover the sending domain and the address; **the inbound domain and the app origin are not listed and must be added** | provide them |
| **OQ-13** | Which beachhead gets the landing page | **PM/HOA.** GC is the outbound programme with a `/for-general-contractors` variant | ask for the GC hero |
| **OQ-14** | Status without a human accepting the reading | **Yes, above τ, from the first document** — `specs/03` §8; the guard is τ + the quote gate + the ≤ 2% confident-wrong ceiling. `UX.md` §8.1 is superseded | require review on document #1 and accept the slower activation |
| **OQ-15** | Shared gap-report link expiry | **30 days, revocable, logged, max 90** — `specs/12` §8 already implements it. Closed | change the default |

---

## 5. What the reviewer should check first on re-review

1. **`specs/15` §6.1** — the eight-condition reconciliation is the one place I deviated from a written
   constraint on purpose, and it is where I most want to be argued with.
2. **`specs/05` §2.1** — the engine-value → status-state mapping, and specifically the `expired` cell,
   which is the residual B-03 item.
3. **`specs/00-event-vocabulary.md` §4** — the retired-names table. If a name was retired that some
   document still needs, this is where it will show.
4. **`LANDING_SPEC.md` §14.2** — run the script; it should print 413.
5. **`KNOWLEDGE_BASE.md` §A.2 and `kb-samples/MANIFEST.md` C16** — a manifest row without a file is a
   deliberate exception to the manifest's own rule 4, gated by `kb:check`. Worth a second opinion.

---

# Round 2 — closing the re-review (`REVIEW.md` §R3, §R4)

**Author:** wave-1b closing author (PIPELINE stage 6, second iteration). **Date:** 2026-09-03.
**Input:** `REVIEW.md` §R3 (eleven items, I-1…I-11), §R4 (one regression, R-1) and §R6.
**Verdict being answered:** *BLOCKING 0 · MAJOR 3 · MINOR 8 open — all eleven in `IDENTITY.md` plus one
paragraph of `identity/samples.html`.*

**Files edited, and only these:** `IDENTITY.md`, `identity/samples.html`, `specs/03-coi-extraction.md`,
`REVIEW_RESPONSE.md`, `identity/CLAUDE.md`. **No token, colour or typeface was changed** — the Brand
Director's arbitration is final and was treated as read-only. `design-system.css`, `identity/contrast.py`
and `REVIEW.md` were not touched. Nothing was committed or pushed.

## Result

| | major | minor | regression |
|---|---|---|---|
| **Closed** | 3 | 8 | 1 |
| **Open** | 0 | 0 | 0 |

**One residual, declared rather than hidden:** the sixteen `COVERED …` row labels in `IDENTITY.md` §6.5
are *generated* by `identity/contrast.py`, which was outside this pass's permitted file set. Details and
the exact one-line fix are in **I-1** below and are written into `IDENTITY.md` §17 item 8 so they cannot
be lost.

## The eleven items

| id | closed by | what changed |
|---|---|---|
| **I-1** (major) | `IDENTITY.md` §0, §1, §3 Step 4, §4.3, §9, §9.2, §9.4, §11, §12.6, §13.3 | **"Covered" retired from the body.** §9.4's hard rule now reads *"a low-confidence field can never contribute to a **Meets requirements** status"*. §12.6's sort order is now the `specs/05` §3 roll-up precedence and `specs/06` A3's default — **Expired · Gap · Expiring (ascending) · Claimed, not evidenced · Needs review · No certificate · Meets requirements** — and says in the same sentence that "Covered" is not a sort key, a counter or a word. §3 Step 4's owned word is **Meets requirements**, with a new paragraph saying *why* the owned word is deliberately not the buyer's word (UA2's quote is kept verbatim; it is their speech, not our claim). §4.3's approved hero copy is now *"Know which vendors meet your requirements — today, and on the day it matters."* §1 Direction C now names **all seven** states and says *"Seven states, because the comparison engine emits seven (§6.4)"*, and its colour-independence paragraph no longer says "the three hues" (two of the seven carry no hue at all). §0's *"if it is green it is covered"* → *"it meets the requirement"*. §11's *"Gap to Covered"* → *"Gap to Meets requirements"*. §13.3's *"Green is covered in both"* → *"Green means meets requirements in both"*. Every surviving occurrence of the word in the file is now a negation, a quoted correction, the form's own *Covered Autos*, or JTBD prose about the buyer's question — the same class of survivor the reviewer accepted product-side in §R1. |
| **I-1 residual** | *not closed — declared* | §6.5's sixteen `COVERED …` row labels (eight `PAIRS` entries × two themes) are printed by `identity/contrast.py` lines 157-198. §6.5 is generated and **must not be hand-edited** — the next `--md` run would revert it, and hand-typing a generated table breaks `IDENTITY.md`'s own amendment rule. `contrast.py` was outside this pass's permitted file set, so the fix is written into §17 item 8 instead: change the eight `COVERED` strings to `MEETS` and re-run `--md`. It moves **no ratio, no token and no colour** (`--c-ok-*` is vocabulary-neutral) and it cannot fail the suite. |
| **I-2** (major) | `IDENTITY.md` §4.4 rule 4, §12.11; `identity/samples.html` L489-492 **and** the gap-report footer | **The second disclaimer text is gone; there is now exactly one.** §4.4 rule 4 is a **pointer** — *"the primary disclaimer, `KNOWLEDGE_BASE.md` §F.1, rendered verbatim from `src/lib/kb/disclaimers.ts`. This document does not restate it"* — and names the two `specs/13` §12 tests it now passes, including the near-duplicate grep. §12.11's gap-report footer points at §F.1 too. In `samples.html` the `c-note` **renders KB §F.1 verbatim**. **A second near-duplicate the review had not flagged was found in the same file** — `c-report__disclaimer`, *"…It is not insurance advice, it does not verify the underlying policy…"* — and was replaced with the §F.1 string as well; it is surface 7 in KB §F.4 and it would have failed the same grep. `grep -n "insurance advice\|does not verify the underlying"` over `IDENTITY.md` and `samples.html`: **0 hits**. |
| **I-3** (major) | `IDENTITY.md` §12.2 (rewritten), §11, §12.4, §4.3, P6 | **The highlight boxes are gone; the `UX.md` §3.2 contract is in their place.** §12.2 now states that the extractor returns `page` + `source_text` and **no geometry**, quotes `specs/03` §3, refuses to add coordinates to the schema (§3 Step 9), and specifies: click/focus a field → **scroll the document pane to that field's `page`, nothing drawn on the page**; the evidence is **in the panel** as the `source_text` quotation in `--c-font-num` with the page number and the **quote-gate result in words** (the three sentences from `UX.md` §3.2); the roving-tabindex list is the **field list**, not boxes. §11's motion bullet now animates **the panel row**, not a box, and says *"nothing animates on the document, because there is nothing drawn on the document to animate"*; the reduced-motion bullet follows. §12.4's *"Every value links to its highlight"* → page number + quotation + scroll-to-page. §4.3's low-confidence example says *"Check it against the quotation beside it"*. **P6** is restated the same way and its review test still passes, which is the point. |
| **I-4** (minor) | `IDENTITY.md` §12.1; `identity/samples.html` | `vendor-name@in.certly.app` → **`vendor-name@{INBOUND_DOMAIN}`**, with the reason written in so it survives: `certly.app` is somebody else's parked placeholder (§2.1 `[F4]`) and `PLAN.md` D3 ships no custom domain. The two literal domains left in `samples.html` (`northgate@in.certly.app`, `in@certly.app`) went with it; `grep certly.app` over `samples.html` is now **0 hits**. |
| **I-5** (minor) | `IDENTITY.md` §7.1 | **`next/font`, self-hosted at build time, zero third-party requests** — `LANDING_SPEC.md` §10's CI-enforced budget, stated as a rule that binds **every shipped surface** (app, landing page, email). Both faces are SIL OFL, so self-hosting is licensed; the `@font-face` rules are inlined in the critical CSS, `display: swap`, `latin` subset, same weights. **The documented exception is named and bounded:** `identity/samples.html` is a framework-free proof page with no build step and is not a shipped surface, so it keeps the Google Fonts stylesheet and is *the only file in the repo permitted to reference a font CDN* — which its own footer already says. Fallback stacks unchanged. |
| **I-6** (minor) | `IDENTITY.md` §4.3 | *"$99 a month up to **50 tracked vendors**. $199 to 150. **$299 to 400**."*, with a line naming the source of both corrections: the unit is `specs/10` §2 / `OFFER.md` §8.1 (B-10) and the ceiling is `OFFER.md` §12.1's `vendor_limit=400`. Quoted from the Stripe metadata, not from memory. |
| **I-7** (minor) | `IDENTITY.md` §12.10; `identity/samples.html` | The composer no longer draws its own ladder. It is `specs/07` §2's, quoted: **T−60, T−30, T−14, T−7, T−1, T+1 (lapsed), then weekly to T+28, then stop and flag** — ten rungs, each a toggle. The **"This is message {n} of {total}"** sentence `specs/07` §6 requires is now specified, along with the caps (`specs/07` §9: **6 per recipient per expiry, 10 per expiry**) and the requirement that `{total}` be shown *before* anything is scheduled — which is what actually answers Procore's 74 days. `samples.html`'s composer was showing the retired −30/−14/−3/+1 offsets and *"sends 3 emails"*; it now shows the canonical rungs and *"This is message 1 of 10 about this certificate."* |
| **I-8** (minor) | `IDENTITY.md` §9.2 | The coverage-bar `aria-label` is now `UX.md` §4.2 / `specs/06`'s sentence verbatim — *"General liability: a policy is shown from 1 January to 12 September 2026, then **no certificate on record**."* — with the rule spelled out next to it: **"no certificate on record", never "no coverage on record"** — the bar states what *we* have, not what the vendor has. |
| **I-9** (minor) | `IDENTITY.md` §12.1 | **Forward-by-email is `SH-1`, not MVP**, so at launch the drop zone shows **no** forward-to address; the MVP paths are named (drag-drop, picker, paste, CSV import, the `specs/08` no-login link), and the env-placeholder form is given for when SH-1 ships. `samples.html`'s empty state no longer promises forwarding either. |
| **I-10** (minor) | `IDENTITY.md` §17.5 | Struck through and marked **RESOLVED 2026-09-03 (MJ-12, OQ-3), and the answer is no**, in `PERSONA.md` §9.4's own words, citing `BACKLOG.md` N12 + `OFFER.md` §9, with the consequence for this document stated: §4.3's pricing block and the empty state are designed against a card-required 14-day trial plus the Free Gap Report, not against a free tier. |
| **I-11** (minor) | `IDENTITY.md` §17.7 | Recorded rather than left looking missed. `specs/05` §2.1's mapping is written out: `undetermined` is the **requirement-level data state**, "Needs review" is the **display state** in §6.4, and the identity keeps its word *because the mapping says to*. Nothing changes in `contrast.py`'s `STATUS_MARKS` or `samples.html`. The drift rule is added: a **document** in `needs_review` (`specs/03` §8) and a **requirement** that is `undetermined` (`specs/05` §2) are different facts wearing the same pill, and any screen that can show both says which it means. |
| **§17 item 8** | `IDENTITY.md` §17 | Rewritten from *"retired in the identity files only"* to **CLOSED**, recording that the product sweep was verified by the reviewer's own `grep` and that this document's body was the residual — plus the one declared exception above. |
| **R-1** (regression) | `specs/03` §15 | Header fixed to **"21 fixtures: 17 real documents (G1–G17) + 4 synthetic (G18–G21)"**, quoting the two files it forbids from disagreeing (both already correct) and naming the off-by-one it had absorbed: **sixteen real fixtures live in `kb-samples/certificates/`, the seventeenth (G16/E1) in `kb-samples/endorsements/`**. The closing line *"The five synthetic fixtures…"* → **four**, with a sentence stating that `D` sums over G1–G17 only, so the count is arithmetic and not a caption. The table itself (21 rows) was already right and was not touched. |

## One decision that needed making: the `expired` row in §6.4

`REVIEW.md` B-03 was closed in §R2, but `specs/05` §2.1 still carries *"One open item for the Brand
Director… the seven status states have no row for `expired`"*, and it offers two ways out. **The ruling
taken, and written into `IDENTITY.md` §6.4: no eighth state.**

- An **expired** certificate and a **short limit** are the same severity to the person who has to act,
  and §6.1 allows no further hue. `expired` therefore renders in the **`gap` ramp with its own word** —
  exactly the behaviour `specs/05` §2.1's mapping cell already specifies, **so that spec needs no edit**,
  which matters because it was outside this pass's permitted files.
- §6.4 now carries an explicit `expired` row showing it borrowing `gap`'s glyph, pattern and hue and
  owning **the word** — the signal that separates a *lapse* from a *shortfall*, and the one that survives
  a photocopier. §12.6 sorts it **above** `gap`, per `specs/06` A3.
- **`contrast.py` is unaffected and still certifies seven states**: `expired` declares no new token, no
  new glyph and no new pattern, so there is nothing new to certify — and `"Expired" ≠ "Gap"`, which is
  what the word-uniqueness hard-fail exists to protect. Adding it to `STATUS_MARKS` would have *failed*
  the suite (duplicate glyph and pattern), which is itself the argument for the word-variant reading.

## Checks re-run after the edits

| check | result |
|---|---|
| `python3 phase-4-revenue/certly/identity/contrast.py` | **exit 0** — *"All 166 declared pairs pass: 62 contrast + 21 greyscale, × 2 themes"*, *"glyph, fill pattern and word are distinct for all 7 statuses"* (both themes) |
| `python3 phase-4-revenue/certly/identity/contrast.py --css` | **exit 0** — *"every palette token and light value is present in design-system.css"* |
| `python3 phase-4-revenue/scripts/identity-distinctness.py` | **exit 0** — *"All checks pass: 3 apps, 3 ground pairs, 3 typeface pairs"* |
| `grep -ni covered IDENTITY.md`, survivors classified | every one is a negation, a quoted correction, *Covered Autos*, JTBD prose, or the §6.5 generated labels declared above |
| `grep -n "insurance advice\|does not verify the underlying"` over `IDENTITY.md` + `samples.html` | **0 hits** — one disclaimer text, and it lives in KB §F |
| `grep -n "certly\.app"` over `samples.html` | **0 hits** |
| `samples.html` external resources | **3, all Google Fonts** (2 `preconnect` + 1 stylesheet) and the relative `../design-system.css`. Nothing else. Tag balance re-checked; the page still parses clean. |
| `specs/03` §15 table row count | **21** (G1–G21), matching the corrected header, `KNOWLEDGE_BASE.md` §D.5 and `THRESHOLDS.md` §4.1 |

## What a re-reviewer should check first

1. **`IDENTITY.md` §6.4's `expired` row** — the one item above that required a decision rather than a
   copy edit, and the one place a reviewer may reasonably prefer the other branch (`specs/05` §2.1 says
   an eighth row would cost nothing but that mapping cell). The counter-argument is written next to it.
2. **`IDENTITY.md` §12.2 against `UX.md` §3.2** — the two must now say the same thing in the same order.
3. **`IDENTITY.md` §17 item 8** — the declared `contrast.py` residual. It is the only piece of I-1 not
   done, and it is one line in a file this pass was not allowed to edit.
