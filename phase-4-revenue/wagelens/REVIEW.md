# WageLens — WAVE-1B ADVERSARIAL REVIEW

**Reviewer:** wave-1b Reviewer (WageLens). **Date:** 2026-09-03.
**Scope reviewed, in full:** `PERSONA.md`, `IDENTITY.md`, `design-system.css`, `UX.md`,
`identity/samples.html`, `BACKLOG.md`, `specs/WL-00`…`WL-13`, `KNOWLEDGE_BASE.md`, `kb-samples/`
(skimmed), `THRESHOLDS.md`, `OFFER.md`, `LANDING_SPEC.md`, `offer/RESEARCH.md`, the three memories
(`identity/`, `product/`, `offer/`), plus `../PLAN.md`, `../PIPELINE.md`, `../PREREQUISITES.md`,
`phase-1-ideation/ERRATA.md`, and — for cross-app distinctness — `../stateready/IDENTITY.md`,
`../stateready/design-system.css`, `../certly/IDENTITY.md`, `../certly/design-system.css`.
**Rule obeyed (PIPELINE.md):** the reviewer never edits the documents reviewed. This file and
`review/CLAUDE.md` are the only files written.
**Failure rule applied:** where two documents contradict and neither is clearly right, the option
that reduces founder liability wins, and the finding says so.

---

## Verdict

> **9 blocking · 19 major · 10 minor.**
>
> This is strong work — the persona, the knowledge base and the specs are the best-evidenced
> documents in the phase-4 fleet, and I re-verified their three load-bearing facts myself (below).
> It does **not** sign yet. Three of the nine blocking findings sit on the same fault line: **the
> product's only genuine differentiator — pinning the modification the contract locked — is sold on
> the landing page, priced into the offer, and forbidden by the spec and unbuildable from the corpus
> as designed.** Two more are money-path liabilities (a tier sold before it exists, an uncapped
> refund sentence, and no auto-renewal disclosure on a card-on-file trial). One is the cross-app
> identity collision, which is not this fleet's to fix.

### What I re-verified at the source rather than taking on trust

| claim | document | my result |
|---|---|---|
| WH-347 is a flat PDF, sha256 `fa28f033a825…`, 2 pages, 0 form fields | KB F2 / KB-6 | **Confirmed.** `curl` → HTTP 200, 304,738 bytes, sha256 matches byte-for-byte; `pypdf` → `pages 2 fields 0`; text layer prints `Rev. January 2025`, `OMB No.: 1235-0008`, **`Expires: 01/31/2028`** |
| Superseded modifications stay retrievable | KB-3, OFFER §2.2 lever 4 | **Confirmed.** `/wdol/v1/wd/TX20260253/history` → rev 1 `active:true`, rev 0 `active:false`; `/wdol/v1/wd/TX20260253/0` → HTTP 200, 16,319 bytes, full `document` text. **This is what makes B3/B4 fixable rather than fatal.** |
| 72 contrast pairs pass | IDENTITY §6.4 | **Confirmed.** `python3 identity/contrast.py` → "All 72 pairs pass (39 light, 33 dark)", exit 0 |
| Banned figures/phrases absent from copy | PERSONA §6.2, ERRATA E3 | **Confirmed.** `$13,508`, "strict liability", "audit-proof", "100% accurate", "guaranteed compliant", "seamless", "effortless" appear **only** inside banned lists or quoted competitor copy |
| Every analytics event named in `THRESHOLDS.md` exists in a spec | THRESHOLDS §1–§5 | **Confirmed** for all 16 (`lookup_performed`, `lookup_cta_clicked`, `signup_completed`, `checkout_completed`, `wd_pinned`, `classification_mapped`, `classification_zero_results`, `wd_search_ambiguous`, `wh347_generated`, `payroll_certified.minutes_in_grid`, `payroll_copied_from_last_week`, `subscription_activated`, `subscription_cancelled`, `wd_alert_email_sent`, `official_determination_link_clicked`, `ssn_full_entry_blocked`). **The landing page's events are a different story — see B6.** |

---

## 1. Findings

Severity: **blocking** = wave 2 must not build it as written, or it is a founder liability.
**major** = the build will stall or guess. **minor** = fix before launch, not before build.

### 1.1 Blocking

| id | severity | file · section | finding | evidence | required change | owner |
|---|---|---|---|---|---|---|
| **B1** | blocking | `UX.md` §1, §6 (A16), §13 Q1 **vs** `OFFER.md` §7/§10, `BACKLOG.md` WL-09 + §4, `specs/WL-09` §Story, `THRESHOLDS.md` §0.3 | **Two incompatible trial designs.** UX says the first week is free, unpaid and cardless, with the paywall at the *second* week's form. Everything else says a 14-day card-on-file trial charged on day 15, and BACKLOG lists "a free tier" under **Never**. | UX §1: *"Payment is required to **generate the second week's WH-347**: the first week is free, complete and downloadable."* · UX §6 A16 empty state: *"You're on the free week."* · OFFER §7: *"Your first two Fridays are free. Card on file, charged on day 15."* · WL-09: *"if I do nothing I am charged $99 on day 15"*, `payment_method_collection=always` · BACKLOG §4 Never: *"A free tier … A 14-day card-on-file trial is the free tier."* | Adopt the **14-day card-on-file trial** (decision D1 below). Rewrite `UX.md` §1 (journey + paywall paragraph), §6 A16 empty state, and §13 Q1. Note the second, smaller contradiction inside the same finding: "the first week free" (one form) vs "your first two Fridays are free" (two) — the offer's wording is the one that ships. | identity (UX), confirmed by offer |
| **B2** | blocking | `LANDING_SPEC.md` §8 **vs** `specs/WL-09` §Screens + Edge cases, `BACKLOG.md` §2 (WL-24) | **The $299 GC Roll-up tier is sold on the landing page before the product exists.** The pricing table gives it a live `Start free` CTA and OFFER §10 creates six purchasable Stripe prices; WL-09 says it is "sellable only when WL-24 ships" and `/pricing` must show "coming soon — join the list"; WL-24 is a **Should**. | LANDING §8 table row: `CTA | Look up a rate | Start free | Start free | Start free` · WL-09 §Screens: *"the GC tier marked 'coming soon — join the list'"* · WL-09 Edge cases: *"A GC asks for the $299 tier before WL-24 ships → `/pricing` shows it as 'coming soon'"* · BACKLOG §2: WL-24 effort **L**, trigger "3 paying subs on one GC's project, or 5 inbound GC requests" | Landing's GC card becomes **"Coming soon — join the list"** with a waitlist capture emitting `gc_tier_interest`; no `Start free` CTA; feature bullets stated in the future tense. `OFFER.md` §10: create `wagelens_gc_*` prices in **test mode only** and mark them "not exposed in Checkout until WL-24 ships". Selling a subscription whose named features (sub seats, weekly collection, per-sub status board) do not exist is a refund/chargeback and misrepresentation exposure. | offer (landing + Stripe list), product (WL-09) |
| **B3** | blocking | `specs/WL-02` V3 + acceptance criterion **vs** `specs/WL-02` Edge cases **vs** `OFFER.md` §1/§3.2 O2b+O7/§8 Q2, `LANDING_SPEC.md` §1/§5.1 | **The spec forbids the one thing the offer sells.** WL-02 V3 requires the pinned determination to be `is_active = true`, and its acceptance criterion says a superseded modification "does not pin". The edge-case table in the same spec says the opposite. Contract-modification pinning is, per OFFER §11.1 and RESEARCH §3.6, *the only genuinely unheld ground in the offer*. | WL-02 V3: *"The pinned determination must exist … and be `is_active = true` at pin time \| blocked with the active alternative offered"* · WL-02 AC: *"… offers the active modification, and **does not pin the superseded one**"* · WL-02 Edge case: *"The user's contract names a modification **older** than the active one \| **Allowed**, and it is a real and correct case … `resolveDeterminationByNumber` accepts an explicit modification number and **pins it**"* · OFFER §3.2 O2b: *"**CORE — and the only obstacle on this list no incumbent page claims to solve. This is the offer.**"* | Delete the `is_active` clause from V3 and rewrite the acceptance criterion: *given an explicit `(wd_number, modification_number)` that exists in the corpus, it pins, with a permanent "a newer modification (n) was published on {date}" state on the card and on every payroll header.* Keep the current behaviour only for the case where the user typed a bare WD number with no modification (then offer the active one). Authority for allowing it: 29 CFR 1.6 fixes the applicable determination at solicitation/award. **Refusing to pin the contract's own modification forces the customer to file at a rate their contract does not carry — the exact harm the product is sold against.** | product |
| **B4** | blocking | `KNOWLEDGE_BASE.md` §2 KB-1 + §4.1, `specs/WL-13` Flow/AC **vs** `OFFER.md` §2.2 lever 4/§3.2 O2b, `LANDING_SPEC.md` §5.1 + V2, `specs/WL-00` | **The corpus never ingests superseded modifications, so pinning, the timeline and three-year reproducibility cannot render at launch.** Ingestion reads the index with `is_active=true` and fetches only new *active* pairs; `/history` is never called except as a secondary change-detection idea in KB §6.3. KB-3's "retrievable forever" is a property of SAM.gov, not of our database — but every user-facing promise reads from our database (WL-02 `searchDeterminations` "**never the network**"; WL-03 V1). | KB §4.1 step 1: *"INDEX 3 × GET size=2000 is_active=true"*; step 2 diff: *"new pair → fetch"* · WL-13 AC: *"`kb_wage_determinations` holds ~4,235 **active** rows"* · LANDING §5.1: *"Choosing a modification **re-renders the whole table at that modification**"* and §14: *"The modification control ships in v1 — without it the page has no differentiator"* · LANDING V2 data: *"real, from `…/wdol/v1/wd/{ref}/history`"* | Add to `specs/WL-13`: (a) a `kb.fetch_history` job that stores every `(wd_number, modification_number, publish_date, active)` row for a determination the moment any project pins it, any visitor opens `/wd/:wdNumber`, or a watch is created; (b) an on-demand `kb.fetch_determination` for a superseded revision, with the same gates G1–G4; (c) a launch backfill for the demo determination(s) the landing page ships with; (d) an acceptance criterion asserting a superseded revision is stored and readable. **Verified feasible during this review:** `/history` returns both revisions and `/wd/TX20260253/0` returns HTTP 200 with a 16,319-byte document. | product |
| **B5** | blocking | `OFFER.md` §6.1 (free tier) + B3, `LANDING_SPEC.md` §5.1 + §13 (`alert_email_captured`), `UX.md` §3 (P2) — **no spec, no owner** | **The free "watch this determination" alert is promised on three surfaces and specified nowhere.** WL-08's alerts are keyed to a *project* (`wd_change_alerts.project_id`, V7 "projects with `status = 'active'`"). `KNOWLEDGE_BASE.md` §3.2 lists a `wd_watches` table that no spec defines. There is no MVP item, no consent record, no unsubscribe design and no privacy copy for collecting an email address on a public page. | OFFER §6.1 Rate Lookup row: *"**Email only if you want alerts:** 3 saved determinations, change alerts, the Conformance Pack"* · LANDING §5.1: *"'Email me when this determination changes' → email, up to 3 saved determinations, free forever"* · UX §3 P2: *"One primary action: **'Watch this determination'** → email capture → E7 when it changes … needs no account"* · KB §3.2 lists `wd_watches`; `grep` finds no spec that owns it | Either (a) write **WL-14 · Public determination watch** as a Must — `wd_watches` (email, wd_number, created_ip_hash, confirmed_at, unsubscribe_token), explicit consent capture, confirmation email, ≤3 per address, one-click unsubscribe, CAN-SPAM footer with the P10 postal address, and a privacy-page paragraph — or (b) delete the promise from OFFER §6.1/B3, LANDING §5.1 and UX P2 and ship the lookup with no capture. **Recommendation: (a)** — it is the only email list the product builds organically, but it must be built as a consented list, not a scrape. | product (spec), offer (copy) |
| **B6** | blocking | `LANDING_SPEC.md` §13 **vs** `specs/WL-00` Analytics, `specs/WL-09` Analytics, `THRESHOLDS.md` §1 | **The landing page and the specs use two different event vocabularies, so the pre-committed funnel cannot be computed.** THRESHOLDS §1 is defined on `lookup_cta_clicked ÷ lookup_performed`; the landing page's primary and leading metrics are `trial_started` and `lookup_completed`, and it introduces seven events no spec defines. | LANDING §13: `lookup_started`, **`lookup_completed`** ("the leading indicator"), `source_chip_clicked`, `alert_email_captured`, `plan_cta_clicked`, `hero_viewed`, `faq_opened`, `ledger_used`, `modification_pin_used`, `wh347_artefact_expanded`, `timeline_viewed`, `comparison_table_viewed` · WL-00: `lookup_performed`, `lookup_ambiguous`, `lookup_zero_results`, `lookup_official_link_clicked`, **`lookup_cta_clicked`** · THRESHOLDS §1: *"`lookup_cta_clicked` ÷ `lookup_performed`"* | **The spec names are canonical** because THRESHOLDS evaluates them. Rename in `LANDING_SPEC.md` §13: `lookup_completed` → `lookup_performed`; `source_chip_clicked` → `lookup_official_link_clicked`; `plan_cta_clicked` → `pricing_cta_clicked` **and** add it to WL-09; keep `hero_viewed`, `faq_opened`, `ledger_used`, `wh347_artefact_expanded`, `timeline_viewed`, `comparison_table_viewed`, `modification_pin_used`, `alert_email_captured` **but add them to the WL-00 (and B5's new spec) analytics lists** so one document owns each name. One event, one definition, one owner. | offer + product |
| **B7** | blocking | `IDENTITY.md` §7.1, §14, `design-system.css` **vs** `../stateready/IDENTITY.md` §8.1, `../certly/IDENTITY.md` §type | **All three phase-4 apps landed on Public Sans + IBM Plex Mono on a warm paper ground**, against the founder's requirement of completely different identities per app. WageLens's own §14 reserved "brick 18° + warm bone" against siblings that had not yet been authored; that reservation has failed. | WageLens: `--wl-font-ui: "Public Sans"`, `--wl-font-mono: "IBM Plex Mono"`, canvas `#FBF9F5` · StateReady: `--sr-font: "Public Sans"`, `--sr-font-mono: "IBM Plex Mono"`, `--sr-paper: #FAF8F4` · Certly: `--c-font-ui: "Public Sans"`, `--c-font-num: "IBM Plex Mono"`, `--c-paper: #F3F3EE`. All three justify Public Sans with the same argument ("the typeface of the US Web Design System"). | **Not this fleet's decision.** A Brand Director agent is arbitrating centrally and will write `phase-4-revenue/IDENTITY_ARBITRATION.md` and edit the identity files that must change (the file does not exist as of this review). WageLens must **not** change typeface or ground unilaterally. What this fleet must do: (a) build every component against the semantic `--wl-*` tokens only, never a hex, so a Brand Director change is a token-file swap (`LANDING_SPEC.md` §Palette binding already mandates this — hold that line); (b) on arbitration, re-run `identity/contrast.py` and re-certify all 72 pairs; (c) rewrite `IDENTITY.md` §14 to cite the arbitration outcome instead of a self-declared reservation. | **Brand Director** (arbitration) → then identity |
| **B8** | blocking | `LANDING_SPEC.md` §5 (proof copy) **vs** `OFFER.md` §5.2 G2 | **The landing page's short form of the Provenance Guarantee drops the cap**, turning a bounded 12-month refund into an unbounded promise on the page a court would read. | LANDING §5: *"If a rate does not match the determination we cite, **we refund what you have paid**."* (47-word block) · OFFER §5.2 G2: *"…we refund every month you have paid since that rate first appeared in your account, **up to twelve months**"*, with the founder-liability note *"a twelve-month systematic claim is **≈$237,600**"* | The short form must carry the bound in the same sentence — e.g. *"…we refund the months you paid since that rate appeared, up to three."* — or be cut entirely. The build checklist already cuts it pending legal (§14); make that **unconditional** until the cap wording is fixed, and make the cap number match whatever the founder chooses in Q6 below. A guarantee sentence that contradicts the guarantee page is worse than no guarantee (OFFER's own §5.1 argument, applied to itself). | offer |
| **B9** | blocking | `specs/WL-09`, `LANDING_SPEC.md` §8, `OFFER.md` §7 — **absent from all three** | **No auto-renewal / negative-option disclosure or consent record on a card-on-file trial that auto-charges.** A free trial that converts to a recurring charge is a negative-option offer: the terms must be clearly and conspicuously disclosed **before** the payment method is collected, consent must be express and recorded, and cancellation must be simple (ROSCA; the FTC's negative-option rulemaking; state auto-renewal statutes such as California's ARL, which also requires a renewal reminder for longer terms). Nothing in the documents requires the disclosure, the consent record, or an annual-renewal notice; and the landing CTA on the paid cards reads `Start free`. | LANDING §8 pricing table: `CTA | … | Start free | Start free | Start free` with the trial line placed *below* the cards · WL-09 has no validation rule for disclosure or consent, and no renewal-reminder requirement (only Stripe's own trial-ending email, in OFFER §10) · OFFER §7: *"Card on file, charged on day 15"* | Add to `specs/WL-09` as V14–V16: (V14) the checkout page renders, adjacent to and above the button, the full terms — *trial length, the exact amount and date of the first charge, the renewal interval, and how to cancel* — and the button reads **"Start 14-day trial"**, never "Start free"; (V15) acceptance of those terms is recorded (`subscriptions.terms_version`, `terms_accepted_at`) the same way `disclaimer_acknowledgements` works in WL-11; (V16) an annual plan sends a renewal notice ≥7 days before each renewal, and the day-10 trial email (OFFER §7) becomes a spec requirement, not a marketing intention. Change the landing CTA labels to match. **This is the cheapest blocking fix in the list and the one with the sharpest downside if skipped.** | offer (copy) + product (spec) |

### 1.2 Major

| id | severity | file · section | finding | evidence | required change | owner |
|---|---|---|---|---|---|---|
| **M1** | major | `UX.md` §4 **vs** `specs/WL-09` Flow | The ten-minute onboarding budget has no Stripe Checkout step, but WL-09 puts Checkout between signup and the product, and a cancelled Checkout leaves the account "read-only" — i.e. no project, no roster, no form. | UX §4 budget: magic link 60 s → company 30 s → project 3 min → roster 3 min → week 2 min → flags 60 s = **≈9.5 min**, no card step · WL-09: *"'Start 14-day trial' ─▶ signup (WL-01) ─▶ `/billing/start` ─▶ Stripe Checkout"*; *"cancel ─▶ /billing/start … account still usable read-only"* | Add "Checkout (card) — 60–90 s" to the UX §4 table and to the §1 journey diagram, and restate the promise honestly (≈11 minutes, or cut 90 s elsewhere). The promise is instrumented (`onboarding_started` → `wh347_generated`); an unmeasurable minute is a broken promise on the dashboard. | identity |
| **M2** | major | `OFFER.md` §2.4, §3.2 O12, §4 B1 + **B6**, §8 Q8 **vs** `BACKLOG.md` §2 WL-15 | **The offer sells an importer the MVP does not build**, and B6 makes it the reason to choose the annual plan. | OFFER §2.4 lever: *"import once and carry forward forever"* · B1: *"A **guided import** of your crew and your first project"* · **B6 "Bring Your Own History": *"Import last quarter's payroll so the 3-year archive starts full … **Annual plans only — a real reason to choose annual**"*** · BACKLOG WL-15 (Should): *"Rosa's hours come off **paper time cards**; a CSV importer solves a problem the 12-person sub does not have."* | Delete B6 from the launch offer (or move WL-15 into Must, which I do not recommend at this scope). Reword B1 to what the MVP does: a guided **type-in or paste** of the crew. Add to `specs/WL-04` a "paste a list of workers" affordance (WL-05 already supports rectangular paste; WL-04 does not, and 15–50 workers entered one drawer at a time will not fit UX §4's 3-minute roster budget). Note for the record: BACKLOG's cold-start argument for deferring import is the weakest in the document — PERSONA §4.1/A4 and UX §5.3 both say import failure is the incumbents' defining defect — but paste + a Should-with-trigger is a defensible launch answer, and selling an unbuilt import is not. | offer + product |
| **M3** | major | `UX.md` §8.2, §3 (A11) **vs** `specs/WL-01` data model, `specs/WL-10` Edge cases, `BACKLOG.md` §3 WL-37 | **Three roles vs one user.** UX makes signing a role-gated action ("the office manager prepares; the owner signs"); the schema has one org-level `role` with `member` explicitly "unused until WL-37", and settings says "one login per organisation for now". | UX §8.2 table: Signer / Preparer / Viewer; §3 A11: *"Only the owner or a user with the **Signer** role can complete it"* · WL-01: `role text notNull default 'owner' // owner \| member (member unused until WL-37)` · WL-10 Edge case: *"A second user is invited \| **Not in the MVP** (WL-37)"* | Decide once. **Recommendation:** UX §8.2 becomes the post-WL-37 design and is labelled as such; the MVP ships one login per organisation and A11's gate becomes "the certifying official's name and title are typed at certification" (which WL-05 B12 already requires). Do **not** ship a half-role model. Keep the seam WL-05 already has (`certified_by_user_id`). | identity (UX) + product |
| **M4** | major | `specs/WL-05` `createPayroll` + AC **vs** `specs/WL-07` Edge cases | The payroll number is allocated at two different moments. This decides whether an abandoned draft burns a number — and gaps in certified payroll numbers are what an auditor looks for first (both specs say so). | WL-05: *"`createPayroll` … **allocates the next `payroll_number`** in a transaction"*; AC: *"when payroll #8 is **created**, then `payroll_number = 8`"* · WL-07 Edge case: *"Drafts never consume a number — the number is allocated **at certification**, not at creation."* | **Allocate at certification** (WL-07 wins): it is the only version in which a deleted draft cannot create a gap in a federal filing sequence. WL-05's `createPayroll` reserves nothing; the draft header shows "payroll #8 (provisional)". Update WL-05's action, AC and the `unique (project_id, filer_organisation_id, payroll_number)` narrative accordingly. | product |
| **M5** | major | `UX.md` §7 **vs** `specs/WL-05` "Keyboard model" | Two different keyboard maps for the same grid — the screen both documents call the product's retention mechanism. | UX §7: `Ctrl/⌘+D` *"Fill down from the cell above, **to the end of the column**"*; `Ctrl/⌘+→` *"Fill the whole **day row** with the standard day"*; `Esc` revert; `S` = split this day; `Ctrl/⌘+Enter` save; `0–9` type-through · WL-05: `Ctrl/⌘+D` *"copy **the cell above**"*; `Ctrl/⌘+→` *"fill the **rest of the workweek** with the current cell's value"*; `.`/`Space` = default daily hours; `Ctrl/⌘+S` save; no `Esc`, no `S` | Reconcile into one table, in `specs/WL-05` (the build reads the spec), and have `UX.md` §7 reference it rather than restate it. **Recommendation:** keep WL-05's semantics (they are the ones with test cases attached) and add UX's `Esc`-reverts and `S`-splits, both of which are genuinely useful and cheap. `hours_keyboard_shortcut_used` must enumerate the final set. | product + identity |
| **M6** | major | `UX.md` §5.2 **vs** `specs/WL-01` | Magic-link parameters diverge on four values, and UX's cross-device six-digit code — the mitigation for the exact failure mode this buyer has (reads email on a phone, works on a desktop, P§10) — is absent from the spec. | UX §5.2: link lifetime **15 minutes**, resend on a **30-second** timer, **six-digit code** for the cross-device case, route `/signin` · WL-01: `expires_at` **20 minutes** (V2), resend disabled **60 s**, no code, routes `/signup` + `/login`, plus a two-step GET→POST to survive Outlook Safe Links | Settle in `specs/WL-01`: **20 minutes, 60-second resend, the two-step verify** (it defeats link-prefetch scanners, which is the more common failure), routes `/signup` and `/login`. Add the six-digit code as a named MVP requirement — UX's argument for it is evidence-backed and it is one column and one input. Update `UX.md` §5.2 to match. | product + identity |
| **M7** | major | `UX.md` §3 (A9 flag table) **vs** `specs/WL-05` W1, `specs/WL-08` | **UX blocks a filing where the spec deliberately warns.** UX makes `rate-below-determination` and `fringe-missing` blocking, and escalates `determination-moved` to blocking after 7 days. WL-05 argues explicitly that blocking a below-determination rate would make us the enforcer of a legal judgement that is the contractor's; WL-08 never blocks. | UX §3 A9: `rate-below-determination` → *"**Yes**"* (blocking); `determination-moved` → *"Warn, **escalates to blocking after 7 days**"* · WL-05 W1: *"**W1 is not blocking, and that is deliberate.** A rate can legitimately be below the determination's headline (an approved conformance, a different modification governing the contract, an apprentice percentage). Blocking would make us the enforcer…"* | **WL-05 wins on liability grounds** (the failure rule): blocking a federal filing on our reading of the customer's legal position is the single most dangerous thing this product could do, and it contradicts G4 and the KB §9.3 disclaimer. Amend `UX.md` A9: `rate-below-determination` = loud warning + recorded acknowledgement; `determination-moved` never blocks. Keep blocking only for what makes the *form* invalid (unmapped worker, missing fringe answer, deductions that do not reconcile — WL-05 B1–B12). | identity (UX) |
| **M8** | major | `UX.md` §2 (A12), §3, §9 (E5), §6, `PERSONA.md` §6 **vs** every spec | **The submission lifecycle has no spec.** UX ships submission statuses, a rejection email and a rejection screen; the only status any spec defines is `draft \| certified \| superseded`. | UX §2 A12: *"History, **status**, re-download"* · UX §9 E5: *"`Northgate rejected week 4 — here's what they flagged`"* · PERSONA §6 vocabulary: *"**filed / accepted / rejected / needs revision** — the states the submission is actually in"* · WL-07 data model: no submission status; `payrolls.status = 'draft' \| 'certified' \| 'superseded'` | Cheapest honest fix, and it also answers UX §13 Q2: add to `specs/WL-07` a nullable `submission_status ∈ {not_sent, sent, accepted, rejected}` with `submitted_at`, `status_note`, set **by the user** (honour system, no integration), plus the E5 email fired from a manual "mark rejected". Either specify it or delete A12's status column, E5 and the vocabulary promise. | product + identity |
| **M9** | major | `OFFER.md` §4 B4/B5 **vs** `specs/WL-07` export formats, `specs/WL-06` `document_share_links` | Two bonuses are sold in a shape the specs do not produce. B4 promises "**a single PDF**"; WL-07 produces a ZIP of PDFs plus `manifest.csv`. B5 promises "a read-only link the GC can **bookmark**"; WL-06's link expires in 7 days. | OFFER B4: *"One click: every week of a project … **as a single PDF**"* · WL-07: `documents_zip` — *"every PDF, named … plus a `manifest.csv`"* · OFFER B5: *"A read-only link the GC can **bookmark**. Stops the Thursday phone call."* · WL-06: `document_share_links.expires_at // 7 days` | Either build a merged Audit Binder PDF in WL-07 (it is a real deliverable and B4 is a good bonus) or reword B4 to "one archive: every form, every determination and a source-and-date manifest". For B5, reword to a **7-day link you can re-issue**, or add a revocable, non-expiring project-scoped link — see M10 before choosing. Entitlement flags `audit_binder=true` / `prime_link=true` already sit in the Stripe metadata (OFFER §10, WL-09), so a customer can be billed for a feature nobody has specced. | offer + product |
| **M10** | major | `specs/WL-06` `document_share_links`, `GET /api/share/:token` **vs** `PIPELINE.md` standing rules, `OFFER.md` B5 | **An unauthenticated URL streams a document containing worker names, last-four identifiers and pay.** The design (hashed token, 7-day expiry, access count) is reasonable; the offer's ambition (permanent, bookmarkable) is not, and nothing revokes a leaked link. | WL-06: *"`GET /api/share/:token` \| unauthenticated stream against a live, unexpired `document_share_links` row"* · OFFER B5: *"a read-only link the GC can bookmark"* | Keep the 7-day expiry, add `revoked_at` and a visible "revoke" control, log accesses (already counted) and state the sharing mechanism in the privacy page (WL-11). Do not let B5's copy promise permanence. The regulation forbids full SSNs on transmittals precisely because this data travels; an unrevocable URL is the same risk in a new wrapper. | product + offer |
| **M11** | major | `LANDING_SPEC.md` §12 **vs** `IDENTITY.md` §7.1, `identity/samples.html` | **The landing page's performance budget forbids what the identity mandates.** One says no font CDN and at most one self-hosted family with system tabular figures; the other loads two families from Google Fonts and makes IBM Plex Mono load-bearing for every rate and the whole rendered WH-347. | LANDING §12: *"**Third-party scripts: Zero.** … no font CDN"*; *"at most **one** self-hosted family … use a system tabular-figure feature rather than shipping a second face"* · IDENTITY §7.1: *"One `<link>` to `fonts.googleapis.com` … preconnect to `fonts.gstatic.com`"*; §7.3 the WH-347 is set in mono · `samples.html` lines 11–13 load exactly that | **Self-host both faces as subset WOFF2** (no CDN — it also removes a third-party request from a page whose argument is precision, and a privacy footnote), and keep the mono **only** where the identity says it is load-bearing: V1's rate figures and V5's rendered form. Amend LANDING §12's font row to "two self-hosted subsets, ≤ 45 KB total". A system tabular figure cannot render V5's `$12.25/.40` column alignment credibly. | identity + offer |
| **M12** | major | `UX.md` §9 (E1), `identity/samples.html` ×9, `IDENTITY.md` §11.7 **vs** `OFFER.md` §9, `KNOWLEDGE_BASE.md` §5/§9.2, `specs/WL-11` V7 | **Two product names ship simultaneously in customer-facing artefacts** while the naming decision is still the founder's (PLAN A3, P11). | UX E1 subject: *"`Your **CraftWage** sign-in link`"* · `samples.html`: *"read by **CraftWage** 2 Sep 2026"*, *"**CraftWage** is not a law firm"* · KB §9.2 (printed on every WH-347): *"Generated by **WageLens** on {timestamp}"* · OFFER §9 cold email signs *"— {{sender}}, **WageLens** (a TheVillage company)"* · LANDING_SPEC already does it right: *"every occurrence … is the token `{{PRODUCT}}`"* | Adopt LANDING_SPEC's pattern everywhere: **one `{{PRODUCT}}` token in every user-visible string, one constant in code, the slug `wagelens` unchanged** (PLAN A3), and a one-line "name pending founder decision (PREREQUISITES P11)" banner at the top of `UX.md`, `OFFER.md`, `KNOWLEDGE_BASE.md` §9 and `identity/samples.html`. No document should assert either name in copy until P11 is answered. | identity + offer + product |
| **M13** | major | `LANDING_SPEC.md` §3 wireframe **vs** §4 chosen copy | The wireframe and the chosen copy carry different sub-headlines, and the wireframe's version breaks the hero's own 55-word budget. | §3 wireframe: *"See every classification, base rate and fringe for your county — with the determination number and a link to sam.gov — before you sign up. Then let {{PRODUCT}} fill in Friday's form."* (34 words → hero = 58) · §4 final: *"See every classification, base rate and fringe for your county — then file a WH-347 that names the determination and modification it came from, in three years as well as this Friday."* (31 → hero = 55 ✅) | Redraw the wireframe with the §4 copy. I re-counted §2's budget table and it is honest — 11+31+5+8 = 55, §5 = 70, §3 = 53, §4 = 105, §5 = 110, §6 = 43, **total 436 against 450** — so the only thing wrong here is that the picture disagrees with the words, and the build will copy the picture. | offer |
| **M14** | major | `LANDING_SPEC.md` §6 V1, §3 **vs** `KNOWLEDGE_BASE.md` F3, `specs/WL-00` V4 | **The page's hero visual has no ambiguous-result state**, though one lookup in eight returns several determinations. WL-00 V4 requires the public surface to show candidates; V1's brief describes only "one determination". | KB F3: *"**1,483 of them — 12.17% — map to more than one active determination**"* · WL-00 V4: *"Ambiguous results (F3) show candidates in public exactly as they do in the app — the public surface must not be more confident than the product."* · LANDING V1: *"**Shows:** one determination rendered the way the determination itself prints it"* | Add a V1b brief: the candidate list — WD number, construction type, modification, publication date, **county list as the discriminator**, classification count, no default selected — with one line of copy explaining why there is more than one answer and that the contract decides. Without it, 1 visitor in 8 meets an undesigned screen on the page's most important element, and the hero H1's singular promise ("Your county's Davis-Bacon rate") is contradicted at first click. | offer |
| **M15** | major | `OFFER.md` §3.2 O2, §4 B3, §6.3, §8 Q1 **vs** `OFFER.md` §1 (its own rule), `PERSONA.md` §6, `LANDING_SPEC.md` §14 | The offer breaks its own vocabulary rule in customer-facing answers, using "revision" where the buyer, the form and the determination all say "modification". | OFFER §1: *"All customer-facing copy uses 'modification'; 'revision' appears **only** where the SAM.gov API field is literally `revisionNumber`."* · OFFER §8 Q1 (answer copy): *"the determination number, **revision** and effective date"* · §3.2 O2: *"naming what moved and both **revision** numbers"* · §6.3: *"the **revision**: pinning, history"* | Replace "revision" with "modification" in every quoted answer and bonus description in `OFFER.md`; keep it only in schema/API references (`revisionNumber`). Ten-minute fix, and it is the differentiator's own word. | offer |
| **M16** | major | `LANDING_SPEC.md` §5.2 **vs** `specs/WL-00` Errors, `OFFER.md` §5.2 G2 | The landing widget's fallback serves a **shipped JSON snapshot** of ~200 determinations when the corpus is unreachable; WL-00 fails closed with a link to SAM.gov. A snapshot is a rate we cite without holding its current source — precisely the fact pattern G2 refunds on. | LANDING §5.2: *"**Fallback (demo only):** a shipped JSON snapshot of ~200 high-traffic determinations … the widget serves the snapshot **and says so**"* · WL-00 Errors: *"Corpus unavailable → 'We can't reach our determination data right now. **Search SAM.gov directly →**'"* | **WL-00 wins.** Delete the snapshot fallback. The corpus is our own database (not a third party at request time — LANDING §5.2 says so two bullets earlier), so "unreachable" means our database is down, and a stale rate shown during our own outage is a liability, not a courtesy. Keep the honest error and the SAM.gov link. | offer |
| **M17** | major | `UX.md` §11 (Billing) **vs** `specs/WL-09` V6, `specs/WL-07` Edge cases, `OFFER.md` G3 | Three different promises about what happens to the archive after cancellation. | UX §11: *"Downgrade and cancellation **never delete filed reports**: the archive stays readable and exportable"* (no limit) · WL-09 V6 / `read_only_until`: *"reading and exporting history stays available **for 30 days**"* · OFFER G3: *"Your archive stays downloadable for **30 days** after you leave."* | **30 days wins** (it is what the customer is promised in writing and what the schema implements). Amend `UX.md` §11 to state the 30 days *and* the customer's own three-year retention duty, with a forced export in the cancel flow (WL-10 already forces one on deletion). Do not let the app imply indefinite free storage of federal records. | identity |
| **M18** | major | `LANDING_SPEC.md` §4–§8, `OFFER.md` throughout **vs** `PERSONA.md` §1.2, §14 | **The offer and the page speak only to the owner ("he"); the person who does the work, champions the tool and vetoes the purchase is the office manager**, and PERSONA's own instruction is "sell to the owner; make the office manager the hero". Nothing on the page is addressed to her, and V4 asks the owner for "what an hour of *his* office time costs" — that hour is hers. | PERSONA §1.2: *"The single most important human in this product… She is buying **Friday back**"*; §5.2: *"owner signs; **office manager vetoes**"*; §14 is written entirely in her voice · LANDING §7 §4 Step 02–03 and §8 are written to the buyer-owner; the only Friday reference is the ledger's arithmetic | Add one persona-true line to §4 Step 03 or the pricing kicker that names her work — e.g. *"The person who does this on Friday afternoon types the week once."* — and change V4's third input label from "what an hour of his office time costs" to "what an hour of office time costs you". Costs 6 words inside the existing budget; buys the veto vote. | offer |
| **M19** | major | `../PREREQUISITES.md` P7 **vs** `BACKLOG.md` §4 Never, `KNOWLEDGE_BASE.md` K5 | The founder prerequisites ask for an Anthropic key for a "**WageLens classification assistant**" — the one feature this product refuses to build, on liability grounds, in three documents. | PREREQUISITES P7: *"Anthropic API key … Certly extraction, StateReady playbooks, **WageLens classification assistant**"* · BACKLOG Never: *"**An AI that chooses the labor classification** … the single decision the customer is legally responsible for … **The absence of this feature is a selling point**"* · KB K5: *"no LLM ever proposes a classification"* | Correct P7 to drop WageLens (it needs no model key at launch). Leaving it in the founder's to-do list invites the feature back in wave 2, and PREREQUISITES is the document the founder reads. | orchestrator (PREREQUISITES) |

### 1.3 Minor

| id | severity | file · section | finding | evidence | required change | owner |
|---|---|---|---|---|---|---|
| **m1** | minor | `PERSONA.md` §13 R8 | The OMB expiry quoted for the WH-347 instructions (09/30/2026) contradicts the form's own stamp, and the source is a third-party mirror. | PERSONA R8: *"OMB 1235-0008, **expires 09/30/2026**"*, read from a MoDOT-hosted copy · **My own fetch of the live form:** `Expires: 01/31/2028`, matching KB-6 | Cite dol.gov's WH-347 page for the burden statement and the 18 U.S.C. 1001 line, and note that the mirrored instructions carry an older OMB stamp. The KB's 2028-01-31 calendar item is the correct one. | identity |
| **m2** | minor | `KNOWLEDGE_BASE.md` §5 (stamp) | Every generated WH-347 would print `wagelens.app` — a domain nobody owns and which the naming decision may never produce. | KB §5: *"the generation timestamp, and `wagelens.app`"* · PLAN D3: `*.vercel.app` URLs now; IDENTITY §1: `wagelens.com` is taken by a live pay-equity product | Print `{{PRODUCT_URL}}`, resolved from env to the live host. A wrong URL on a federal filing that must survive three years is a small mistake with a long life. | product |
| **m3** | minor | `THRESHOLDS.md` §4 (table) | The `45–64%` and `< 45%` bands sit under the "Tier mix" row and read as tier-mix thresholds, but their actions ("Ship pause-when-idle", "the flat monthly subscription is the wrong shape") clearly belong to **4b usage retention**. | §4 table row order: `4b Usage ≥65%` → `Tier mix Crew 20–40%` → `45–64% weak` → `<45% failing` | Re-order so 4b's three bands are contiguous and Tier mix follows. A pre-committed number nobody can read is not pre-committed. | product |
| **m4** | minor | `THRESHOLDS.md` §benchmarks / `specs/WL-12` `getRevenue` **vs** `specs/WL-09` status enum | `trialing-with-card` is not a status; and counting trials in MRR reports revenue that has not been invoiced. | WL-12: *"MRR from `subscriptions.mrr_cents` where status ∈ {active, **trialing-with-card**}"* · WL-09: `status // trialing \| active \| past_due \| canceled \| incomplete` | Report **MRR = active + past_due-in-grace**, and "trial MRR" as a separate, labelled figure. Fix the status name. | product |
| **m5** | minor | `design-system.css` lines 798, 810, 983 | Three hard `9px` font sizes (document preview column heads, doc footnote, print href suffix) do not scale with the user's root font size, against IDENTITY §7.2 ("expressed in `rem` so the user's browser setting still scales everything") and §10.8 (200% zoom). | `.wl-doc th { … font-size: 9px; }`; `.wl-doc__foot { font-size: 9px; }`; `@media print a[href]::after { font-size: 9px; }` | Move to a `--wl-text-3xs: 0.5625rem` token (or reuse `--wl-text-2xs`) and tokenise the document preview's five hard-coded hexes as `--wl-doc-*` so they enter `contrast.py`'s table. | identity |
| **m6** | minor | `BACKLOG.md` WL-05 analytics | `payroll_certified` is listed without `minutes_in_grid`, which THRESHOLDS P1 is measured on. The spec has it; the backlog does not. | BACKLOG: *"`payroll_certified` (props: payroll_number, worker_count)"* · WL-05: *"`payroll_certified {payroll_number, worker_count, **minutes_in_grid**}`"* | Add the prop in the backlog line so the two documents cannot drift. | product |
| **m7** | minor | `UX.md` §1 | An unsourced conversion statistic is attributed to a PERSONA source key that does not contain it. | UX §1: *"**62% of buyers say the product trial is their top factor** in the final purchase decision (P§13 S27)"* · PERSONA §5.3/§13 S27 contains no such figure | Cut it or source it. This fleet's whole credibility argument is that its numbers survive being checked. | identity |
| **m8** | minor | `specs/WL-11` help slug + `KNOWLEDGE_BASE.md` §9.2/§9.3 | The product name is hard-coded in a URL slug and in three disclaimer texts, which M12's token pass must also cover. | `/help/**what-wagelens**-does-not-do`; *"**WageLens** is an information tool, not legal or accounting advice"* | Include slugs and disclaimer texts in M12's `{{PRODUCT}}` sweep; keep the *slug* stable (`what-we-do-not-do`) so a rename never breaks a link an auditor bookmarked. | product |
| **m9** | minor | `OFFER.md` §1 | The five-line offer is numbered 1–4, then a block quote, then 5 — the list breaks around the vocabulary note. | `OFFER.md` §1 lines 18–33 | Move the vocabulary note out of the list. Cosmetic, but §1 is the paragraph the founder will read first. | offer |
| **m10** | minor | `LANDING_SPEC.md` §9 FAQ 6 | The FAQ names "Audit Binder export" as if it were a shipped feature name; see M9. | LANDING §9: *"Two-click cancel, 30 days of download, **Audit Binder** export"* | Resolve with M9 — one name for one artefact across offer, landing, spec and product. | offer |

---

## 2. Cross-document decisions

Each contradiction above, with the decision a build fleet should act on and the reason in a sentence
or two. Where neither document was clearly right, the option that reduces founder liability wins.

| # | Contradiction | Decision | Why |
|---|---|---|---|
| **D1** | Free first week (UX) vs 14-day card-on-file trial (OFFER/BACKLOG/WL-09/THRESHOLDS) | **14-day card-on-file trial, two Fridays inside it, charged day 15.** UX is amended. | Three documents to one, and the money path, the Stripe metadata and every threshold band are already built on it. It also reduces liability: a cardless free week hands a signed federal document to an unverified stranger, and BACKLOG's "no free tier" argument (4–6% vs 25–35% conversion) is the same conclusion from the commercial side. |
| **D2** | Pin only active determinations (WL-02 V3) vs pin the contract's modification (WL-02 edge case, OFFER, LANDING) | **Pinning a superseded modification is allowed and is the product.** | 29 CFR 1.6 fixes the applicable determination at solicitation or award. A tool that refuses to pin the contract's own modification pushes the customer to file at a rate their contract does not carry — the harm we sell against — and deletes the offer's only unheld ground. |
| **D3** | Corpus holds active determinations only vs the landing page re-renders at an earlier modification | **Ingest history and superseded revisions on demand** (pin, public `/wd/:wdNumber`, watch), plus a launch backfill for the demo determinations. | Verified live during this review that both endpoints work. Without it, the differentiator, the V2 timeline and the "still provable in year three" claim in G2 are all undeliverable — and G2 is a refund promise. |
| **D4** | Landing sells GC tier vs GC tier is a Should | **Waitlist only, no Checkout path, prices created in test mode.** | Taking $299/month for a roll-up that has no subs, no invitations and no status board is misrepresentation with a refund attached; the demand signal (`gc_tier_interest`) is what BACKLOG's own trigger needs anyway. |
| **D5** | UX blocks a below-determination rate vs WL-05 warns | **Warn loudly, record the acknowledgement, never block.** | Blocking makes us the adjudicator of a legal judgement that is the contractor's (approved conformances and apprentice percentages are lawfully below the headline), which contradicts G4, the KB §9.3 disclaimer and PLAN A10's posture. It also risks stopping a statutory filing. |
| **D6** | Determination-moved escalates to blocking (UX) vs never blocks (WL-08) | **Never blocks.** | Same reasoning as D5, plus WL-09 V11's own principle: nothing we do may stop a federal filing deadline. A banner on every draft payroll is enough, and it is already specified. |
| **D7** | Payroll number at creation (WL-05) vs at certification (WL-07) | **At certification.** | It is the only version in which an abandoned draft cannot create a gap in a certified-payroll sequence, and both specs say a numbered gap is the first thing an auditor looks for. |
| **D8** | Snapshot fallback (LANDING) vs fail closed (WL-00) | **Fail closed, with the SAM.gov link.** | The widget reads our own database, so "unreachable" is our outage; serving a rate whose source we cannot currently confirm is the exact claim G2 refunds. An honest error costs one visitor; a stale rate costs a customer's payroll. |
| **D9** | Archive after cancellation: indefinite (UX) vs 30 days (WL-09/WL-07/OFFER G3) | **30 days, stated in the cancel flow, with a forced export offer and a reminder of the customer's own three-year duty.** | It is what the customer is promised in writing and what the schema implements; the alternative is an unpriced obligation to store federal records forever. |
| **D10** | Two names in customer-facing copy | **`{{PRODUCT}}` token everywhere; slug stays `wagelens`; the name is a pending founder decision (P11).** | PLAN A3 says the founder decides; shipping either name in copy pre-empts them, and IDENTITY §1's evidence (wagelens.com is a live pay-equity product) means the working name cannot ship unexamined either. |
| **D11** | Fonts: CDN and two families (IDENTITY) vs no CDN and one (LANDING §12) | **Two self-hosted WOFF2 subsets, no CDN, mono confined to rates and the rendered form.** | Satisfies both documents' actual goals (typographic system; zero third parties; ≤ 250 KB first view) and removes a third-party request from a page whose entire argument is precision. |
| **D12** | Landing events vs spec events | **Spec names are canonical; landing adopts them and every new event gets a spec owner.** | THRESHOLDS is the pre-committed decision instrument; an event it cannot read is a decision that cannot be made. |
| **D13** | Roles (UX) vs one login (WL-01/WL-10/WL-37) | **One login per organisation at launch; UX §8.2 is labelled as the WL-37 design.** | A half-implemented permission model on a product holding wage data is worse than an honest single login, and the certifying-official fields already capture who signed. |
| **D14** | Import sold (OFFER) vs deferred (BACKLOG WL-15) | **No importer at launch; add paste-a-worker-list to WL-04; delete B6 and rewrite B1.** | Selling an unbuilt bonus as the reason to buy annual is the one commercial promise here that would generate refunds in month one; paste covers the spreadsheet case at a fraction of the cost. |
| **D15** | Identity collision across the three apps | **Deferred to the Brand Director's `IDENTITY_ARBITRATION.md`. WageLens changes nothing unilaterally and builds against semantic tokens only.** | Three apps cannot each argue "Public Sans because USWDS"; the decision needs one owner with all three personas in front of them, and it is not this reviewer's to take. |

---

## 3. Build-readiness

### 3.1 Wave 2 can start today

| item | condition |
|---|---|
| **WL-13 corpus ingestion** | Start now — it is the best-verified spec in the fleet and I re-confirmed its endpoints, hash and 0-field finding first-hand. **Design B4's history/superseded backfill in from the first commit**; retrofitting append-only ingestion later is expensive. |
| **WL-11 provenance components + gate G8** | Start now. `<Rate>` / `<ProvenanceLine>` and the G8 CI test are the spine of every other screen and are name- and palette-independent (build against `--wl-*` tokens, per B7). |
| **WL-01 auth** | Start once M6's four parameters are settled (one paragraph of decisions, no research). |
| **WL-03 catalogue, WL-04 roster (+ paste), WL-06 generator, WL-10 settings, WL-12 admin** | Start now. WL-06 in particular has golden-file tests, a byte-equality gate and a verified fixture set. |
| **WL-05 grid** | Start now on the data model; settle M4 (number allocation), M5 (keymap) and M7 (warn vs block) before the validation layer. |
| **`design-system.css` + components** | Build against semantic tokens only. The system is internally consistent and certified (72/72 pairs, exit 0), but the palette and typefaces are **frozen pending B7**. |

### 3.2 Must wait for a fix

| blocked item | waiting on |
|---|---|
| **WL-02 project + determination pinning** | B3 (spec forbids the offer) and B4 (corpus has no superseded modifications) |
| **WL-00's "pick an earlier modification" control, LANDING V2 timeline** | B4 |
| **The landing page build, end to end** | B2 (GC card), B6 (events), B8 (guarantee sentence), B9 (auto-renewal disclosure), B7 (tokens), M11, M13, M14, M16 |
| **WL-09 checkout surface** | B9 (disclosure + consent record + CTA labels), B2 (GC price exposure) |
| **Anything that captures an email on a public page** | B5 (no spec, no consent design, no unsubscribe) |
| **Any outbound copy, email template or PDF footer** | M12 (`{{PRODUCT}}` token) — these are the artefacts a rename is expensive to fix in |
| **Founder-facing Stripe hand-over** | B2, B9, and founder answers to Q1, Q2, Q6, Q7, Q8 below |

---

## 4. Consolidated founder questions, with my recommended default

Deduplicated from `PERSONA.md` §12, `IDENTITY.md` §15, `UX.md` §13, `OFFER.md` §11.3,
`THRESHOLDS.md` §8, `KNOWLEDGE_BASE.md` §11 — plus three this review adds. **Every one has a
default, so nothing waits on an answer.**

| # | Question | Recommended default | Consequence if the founder says nothing |
|---|---|---|---|
| **Q1** | Approve the rename to **CraftWage**? (`wagelens.com` is a live pay-equity product) | **Yes** — register `craftwage.com`/`.io`/`.app`, clear at USPTO before commercial use, keep the slug `wagelens`. Reserve **ChalkWage** if clearance fails. | Build proceeds with `{{PRODUCT}}` tokens; nothing ships to a customer until this is answered (M12). |
| **Q2** | Sell the **GC tier** at launch? | **No.** Publish it as "coming soon — join the list"; build WL-24 on BACKLOG's trigger. | Waitlist only (D4). |
| **Q3** | The answer to "I only do one public job a year" | **Crew at $79 now**, and pre-approve **pause-when-idle** as the committed remedy if THRESHOLDS §3/§4b fires. | Crew ships; the remedy is blocked until answered — which is why Q15 matters. |
| **Q4** | State e-filing (CA eCPR / WA / NY / IL) at launch? | **No** (PLAN A11). Say so on the page — FAQ 5 already does. | Federal only; the honest line stays. |
| **Q5** | How much of the conformance workflow to build? | **The worksheet only. Never file, never propose a classification or a rate.** As specified in WL-04 and G4. | Ships as specified. |
| **Q6** | **G2 refund cap: 12 months or 3?** | **3 months, service-shaped** — refund the affected months *and* re-issue every corrected form free. Worst case ≈$59,400 at 200 accounts instead of ≈$237,600. | G2 does not ship (Q7), so the landing page runs at 93 words in §5. |
| **Q7** | Does G2 ship before a lawyer reads it? | **No.** Add "counsel review of G2 wording" to PREREQUISITES. | The guarantee sentence is cut from the page (already in the build checklist) — and must be cut anyway until B8 is fixed. |
| **Q8** | Run **Founding 50** (24-month price lock)? | **No**, unless the remaining counter is wired to the real account count. Honest scarcity or none. | Not run. |
| **Q9** | Dark mode at launch? | **Keep it.** ~4% of front-end effort; the tokens and all 33 dark pairs are already certified. | Ships. |
| **Q10** | Is the trial's WH-347 fully filable or watermarked? | **Fully filable during the trial.** Drafts are watermarked `DRAFT — NOT FOR SUBMISSION` (WL-06 V1); a certified payroll never is. | As specified. |
| **Q11** | "Mark as filed" honour system, or require proof? | **Honour system**, plus the four-state `submission_status` in M8. | M8 must still be resolved, or A12/E5 are cut. |
| **Q12** | Time zone for the Thursday/Monday emails | **Per company**, set at onboarding A1, one field. | As specified. |
| **Q13** | **The MRR floor** below which this stops regardless of trend (THRESHOLDS F2) | **$500 MRR by day 120 after the first cold email; $2,000 by day 240.** Below either, stop or pivot. | §7's stop conditions stay ratio-only, with no absolute cash floor — the one real gap in an otherwise complete threshold set. |
| **Q14** | Cost per signup / CAC inputs (THRESHOLDS F1) | Value founder time at **$75/hour**, add Resend + Vercel Pro, and cap **CAC payback at 4 months** at a blended ARPU near $99. | "Spend" verdicts in §2 have no budget attached. |
| **Q15** | Is **pause-when-idle** acceptable as a plan? | **Yes — pre-approve it.** Both §3 and §4b name it as the committed remedy. | Two pre-committed remedies become undecidable exactly when they are needed. |
| **Q16** | Risk appetite on undocumented SAM.gov endpoints | **Ship.** G10 pre-flight, the §6.4 recovery procedure and the nightly contract test make a rename look like an outage, not corruption. Keep G2 at three months until the corpus has run a clean quarter. | Ships with the mitigations. |
| **Q17** | SF-1444 field list, state-form acceptance, and whether GC portals accept an uploaded PDF (KB Q2–Q4) | **Founder/browser tasks.** Until verified: no SF-1444 generation (WL-31 stays Later), no state claim, and OFFER Q5's export claim stays hedged exactly as written. | Nothing ships that depends on them. |
| **Q18** | SAM.gov rate limit (KB Q1) | **4 req/s with jitter and an identifying User-Agent**; measure on the first real run. | As specified. |
| **Q19** | **Auto-renewal disclosure and consent wording** (new — B9) | Adopt the disclosure block in B9, change the CTA to "Start 14-day trial", record consent, notify before annual renewals. | A negative-option offer ships without the disclosure it legally needs. |
| **Q20** | Anthropic key for WageLens (PREREQUISITES P7) | **Not needed.** WageLens ships no model, by design (M19). | The founder chases a dependency the product must not have. |

---

## 5. Sign-off checklist

I sign when every line is true. Nine of these are the blocking findings; the rest are the ones I
will re-check by hand.

**Blocking**

- [ ] **B1** `UX.md` §1, §6 (A16) and §13 Q1 describe the 14-day card-on-file trial, and no document says "free week".
- [ ] **B2** `LANDING_SPEC.md` §8's GC card is a waitlist; `OFFER.md` §10 marks the GC prices test-mode-only until WL-24.
- [ ] **B3** `specs/WL-02` V3 and its acceptance criterion allow pinning an explicit superseded modification; the edge-case row and the rules agree.
- [ ] **B4** `specs/WL-13` ingests `/history` and superseded revisions on demand, with an acceptance criterion and a backfill for the landing page's demo determination.
- [ ] **B5** The public determination watch either has a Must spec with consent, unsubscribe and a postal footer, or is deleted from `OFFER.md` §6.1/B3, `LANDING_SPEC.md` §5.1 and `UX.md` P2.
- [ ] **B6** Every event in `LANDING_SPEC.md` §13 exists in a spec under the same name, and `THRESHOLDS.md` §1's ratio is computable from them.
- [ ] **B7** `IDENTITY_ARBITRATION.md` exists, WageLens's typeface/ground position matches it, `identity/contrast.py` re-run passes all pairs, and `IDENTITY.md` §14 cites the arbitration rather than a self-declared reservation.
- [ ] **B8** No refund sentence appears anywhere without its cap, in the same sentence.
- [ ] **B9** `specs/WL-09` carries the disclosure, consent-record and renewal-notice rules, and no CTA on a paid tier reads "Start free".

**Consistency and claims**

- [ ] One trial design, one activation definition (`wh347_generated`, first per organisation), one price ladder ($79/$790 · $99/$990 · $299/$2,990, 14-day card trial, no meter, no setup fee) and one set of limits (3/15 · unlimited/100 · unlimited/unlimited) across `OFFER.md`, `BACKLOG.md`, `specs/WL-09`, `LANDING_SPEC.md` and `THRESHOLDS.md`. *(Verified consistent today except where B1/B2 break it — hold it.)*
- [ ] `{{PRODUCT}}` token in every user-visible string; slug `wagelens` unchanged; no document asserts a final name (M12, m8).
- [ ] No penalty amount, success rate, accuracy rate, customer count, testimonial, logo or federal seal on any surface. *(Verified clean today — keep it that way.)*
- [ ] Every rate surface — app, public lookup, PDF, landing — renders through the provenance component, and G8 is a CI test that covers all four.
- [ ] No copy classifies a worker, files anything, or signs on the customer's behalf; G4's refusal appears on the page.
- [ ] The disclaimer (KB §9.1/§9.2/§9.3) is present on every rate, every document and every public result page, dated.
- [ ] Every outbound-facing message carries the P10 postal address, an unsubscribe that turns off only non-transactional email, and a consent record where the address was collected on a public page.

**Feasibility against the knowledge base**

- [ ] No screen, spec or copy assumes one determination per (state, county, type); the candidate state exists in the app **and** on the landing hero (M14).
- [ ] No document implies a fillable WH-347; every reference is to a generated reproduction with the verbatim Statement of Compliance behind gate G5.
- [ ] No document implies a bulk download or an API key.
- [ ] The superseded-revision path is ingested, stored and tested (B4), and G9 proves a pinned project never reads a newer modification.

**Numbers**

- [ ] `THRESHOLDS.md` §4's bands are unambiguous (m3), MRR excludes unpaid trials (m4), and an absolute cash floor exists (Q13).
- [ ] `identity/contrast.py` exits 0 and the landing word-count script passes at ≤450 words above the pricing block.

---

## 6. What I looked for and did not find wrong

Recorded so the authors do not churn on things that are already right.

- **Evidence discipline.** Source grading in `PERSONA.md` §0 ([R]/[V]/[U], never mixed), the corrected `$13,508`, the refusal to reuse eBacon's testimonial numbers, "price not published" for Points North, and the self-overturning verification pass in `offer/RESEARCH.md` §3.6 are all exactly what PIPELINE stage 3 asks for. The offer agent recording that its own wedge was wrong, in full, is the single best thing in this fleet.
- **The knowledge base.** F1/F2/F3 are the three findings that make this product buildable, and all three survived my re-checking. The gate set (G1–G10) is structural rather than procedural, `document_text` retained as evidence, `(wd_id, line_no)` keying for Minnesota's duplicate labels, and the `(state_code, sam_county_code, county_name)` key for Alaska's reused code are the marks of someone who read the data rather than imagined it.
- **The immutability rules.** Frozen pins on `payrolls`, denormalised label and rates on `worker_classifications` and `payroll_lines`, append-only determinations, reopen-and-supersede instead of edit, and G9's before/after hash test. A signed federal statement is never mutated anywhere in these specs. That is the hardest thing to get right and it is right.
- **Data minimisation.** Last-four-only enforced by the absence of a column, gate G7 walking the schema, the SSN-pattern rejection in WL-04 V2, and privacy regexes over generated PDFs and CSV exports. Being *unable* to leak is the correct answer to O7.
- **Accessibility.** 72/72 contrast pairs verified by a script in the repo, the two-tone focus ring derived from a real failure, real `<table>` semantics for the grid, no placeholder-as-label, 44px targets, `prefers-reduced-motion`, and a document preview that stays white in dark mode for a defensible reason.
- **Landing discipline.** One problem, one promise, one action; 436/450 words above the pricing block (I re-counted the hero and §5 — both exact); proof rules that forbid invented evidence; the honesty clause admitting a competitor is cheaper at one project; and a performance budget with a CI gate. Only the contradictions above stop it being buildable as written.
- **Backlog ruthlessness.** The Rosa test is applied honestly, the "Never" list is argued rather than asserted, WL-08 ships with its own kill threshold attached, and WL-24's cold-start argument is the right call with the right structural concession. The two Must items I challenged hardest — WL-07 and WL-12 — both survive, for mechanical reasons rather than sentimental ones.

---

**Reviewer's memory:** `review/CLAUDE.md`.
**Next step:** authors iterate (PIPELINE stage 6, one variable at a time, with a changelog); this
reviewer re-reads the nine blocking findings and signs, or escalates after three rounds.
