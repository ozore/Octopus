# StateReady — response to the wave-1b review

**Author:** Iteration agent (StateReady), phase-4 fleet. **Date:** 2026-09-03.
**Answers:** `REVIEW.md` — 11 blocking, 19 major, 16 minor, plus decisions D1–D9 and questions Q1–Q18.
**Pipeline stage:** 6 (iteration). The reviewer decides whether this closes the round.

> ## Outcome — **29 fixed · 6 fixed differently · 3 declined · 3 delegated · 1 split**
>
> | | blocking | major | minor | total |
> |---|---:|---:|---:|---:|
> | fixed as asked | 8 | 15 | 6 | **29** |
> | fixed differently | 2 | 2 | 2 | **6** |
> | declined, with reason | 0 | 0 | 3 | **3** |
> | delegated | 0 | 1 | 2 | **3** |
> | split (part fixed, part delegated) | 1 | 1 | 0 | **2** |
>
> **No blocking finding is left open.** B8 is the split: everything the review asked for in
> `UX.md` and `LANDING_SPEC.md` is applied — the identity arbitration landed mid-iteration and is
> final — and the `IDENTITY.md` rewrite itself stays with the Brand Director, who owns that file.
>
> **Evidence, re-run after every edit:** `validate.py` **exit 0** (9 records, 0 failures, 3 warnings
> — the same three correct G7 warnings on Florida reciprocity); `test_accept_drift.py` **17/17
> assertions pass**; landing copy deck **413 words** above pricing against a 450 ceiling, counted
> mechanically under §1's own rule and now including all three CTA placements.
>
> **Four decisions the founder may want to overturn** are listed in §4, each with the lever.

---

## 1. The decisions, as applied

| # | decision | applied where | note |
|---|---|---|---|
| **D1** | **14-day free trial, no card, first 100 signups.** $149 First State Audit and the done-for-you roster build **deferred to iteration 2** behind the register-ingestion spike. Entry Packs unchanged. **H2 in force, H2b registered and out of force.** | all eight files in `REVIEW.md` §1.3 — see B1 | applied verbatim |
| **D2** | The **no-login State Rulebook demo is the single free entry point**; it becomes Must **M15**; the free roster audit stays a SHOULD | `BACKLOG.md` §1/§2, `UX.md` §1/S02, `LANDING_SPEC.md` §0/§12 | applied |
| **D3** | Two guarantees ship, in `OFFER.md` §5.1 wording only; Rollout withdrawn; Alert not shipped; reinstatement-fee never | `OFFER.md` §5, `specs/08`, `specs/12`, `LANDING_SPEC.md` §8/§13 | applied, **and the Alert Guarantee re-drafted** with the five carve-outs and a cap so counsel has something to price (B4) |
| **D4** | Launch ICP = 15–100 technicians, 2–6 states; the 40-state platforms are a wave-3 Entry-Pack motion | `BACKLOG.md` §0 (new subsection), `OFFER.md` §7, `specs/09` "Above the cap" | applied, **and given a route** rather than only a caveat — see M8 |
| **D5** | Widen **states** before trades, in §11.1's order | `KNOWLEDGE_BASE.md` §14 Q1, with the corrected count | applied |
| **D6** | `M14 → M5 → M6`; build M5 first as a pure function with golden tests; spec headers corrected | `specs/04` header (now depends on M5), `specs/05` header, `BACKLOG.md` §5 | applied |
| **D7** | Tile grid; one vocabulary READY / AT RISK / LAPSED / NOT TRACKED; **AT RISK ≤ 90** | `specs/07` rewritten, `specs/02` `/onboarding/states`, `UX.md` S09, `LANDING_SPEC.md` V1 | applied, **and made structural**: a unit test asserts `AT_RISK_DAYS === ALERT_OFFSETS[0]` |
| **D8** | Vercel Pro as a wave-2 prerequisite; hourly drain | **overridden** — `specs/06` is now correct on **one cron a day** and tightens to hourly with no code change | see B9 |
| **D9** | `design-system.css` is the source of truth until the arbitration lands | **superseded mid-iteration**: `../IDENTITY_ARBITRATION.md` is final and committed | see B8 |

---

## 2. Findings, one row each

### 2.1 Blocking

| id | status | files · sections changed | the decision taken |
|---|---|---|---|
| **B1** | **fixed** | `OFFER.md` header/§7/§8/§12/§13 · `LANDING_SPEC.md` §0/§2.1/§2.4/§5/§10/§13 · `specs/09` (the "open disagreement" section replaced by D1) · `THRESHOLDS.md` §3/§7/§8 · `BACKLOG.md` M9 + M15 · `PERSONA.md` §9 · `UX.md` §1 · `README.md` | D1 applied verbatim across all eight files, plus `README.md`, whose front page still advertised the disagreement as open. `THRESHOLDS.md` §7 carries the changelog row: **no band moved**, H2b marked `NOT IN FORCE`, and the reversal procedure written down (edit §7 first, §2 second, before a single audit is sold). `specs/09` keeps `one_off_purchases.kind = "first_state_audit"` as a **dormant enum value** with a test asserting no code path can write it, so iteration 2 is a flag rather than a migration. The "first 100" is enforced by a counter, not aspiration — so the cohort `THRESHOLDS.md` evaluates contains one trial design. |
| **B2** | **fixed** — narrowing chosen | `OFFER.md` §3 (stack items 1–2, the one-sentence promise) + §6.1 · `LANDING_SPEC.md` §2.3 subhead, §4 V4, §12.1/§12.2, §13 · `specs/08` (promise, `entryPackReady` gate, pre-purchase disclosure, AC5b) · `KNOWLEDGE_BASE.md` §9.1 (new, with the counted table) | **The promise is narrowed** to *"every requirement the board publishes, and, on the first page, every requirement it does not"*, applied consistently. **The completeness gate was rejected as the primary instrument** because it would make **zero of the nine records purchasable** — 23 of 23 bond amounts are `unknown` — and would only be liftable by human research, which is the loop `PLAN.md` forbids: it would have converted a marketing problem into a no-revenue problem. A gate still exists and it is a **structural** one: `entryPackReady`, computed at snapshot load and explicitly **not** `publishable`, blocks on a CORE_SET (licence classes, who must hold, cycle, expiry rule, CE, reciprocity statement — all nine records pass) and **discloses** the DISCLOSED_SET (fees, bond, insurance, timeline) **before the Checkout session is created**. Bond and timeline are out of the subhead, out of V4 (the `Bond amount + acceptable forms` step card is replaced by `What the board does not publish`), and out of the demo's default view. |
| **B3** | **fixed** | `BACKLOG.md` §2 **S10** (new) · `OFFER.md` §8/§10 obj. 5/§13 weakness 3 · `LANDING_SPEC.md` §2.4/§6/§11/§13 · `specs/09` | The roster build is deferred and **every surface that promised it is edited**: the hero microcopy is deleted, "How it works" step 2 becomes the CSV import, objection 5's answer is rewritten, and "any claim that we build the customer's roster" is added to `LANDING_SPEC.md` §11's permanent prohibition list. **S10 is written as a SHOULD with acceptance criteria** — 2 dev-days, 15 states, per state: is there a public search, can it be queried by company name, machine-readable or bot-walled, rate-limited, entity or individual licences, does it expose expiry and status; every row with a fetched URL and a date; a written go/no-go and a count of how many of fifteen are automatable; the terms-of-use position recorded, not assumed. A Must for the build only follows a majority-positive verdict. |
| **B4** | **fixed** | `OFFER.md` §5.3 (re-drafted) · `specs/06` edge cases + validation (the suppression reasons) · `specs/12` refund policy + AC8 · `LANDING_SPEC.md` §8/§11 | The Alert Guarantee is **re-worded with exactly the five carve-outs the review lists** — (a) licence added fewer than **100 days** before expiry, (b) muted state, (c) paused notifications, (d) every recipient address bounced or suppressed, (e) trial-end or `past_due` pause — **capped** at the lesser of twelve months' fees and the fees actually paid, **claims within 30 days** of the lapse, and **adjudicated from our own send log**. Each carve-out has a database row behind it: `specs/06` now records every suppressed alert with a machine-readable reason (`added_after_offset`, `muted_state`, `recipient_paused`, `address_suppressed`, `subscription_paused`), so adjudication is a query rather than an argument. **It does not ship at launch** (D3, Q15): `specs/12` AC8 fails the build if its text appears on any rendered surface. Drafting it now means counsel prices it in one reading instead of starting from a blank page. |
| **B5** | **fixed** | `OFFER.md` §5.1.2 · `specs/08` §Guarantee · `specs/12` refund policy · `LANDING_SPEC.md` §8/§13 | **One wording, with a verifiable standard:** *"If a page published by the state's own licensing board contradicts a value your State Entry Pack shows as verified, tell us within 90 days of your purchase and we rewrite the pack and refund what you paid for it. We adjudicate against the board's published page, not against a conversation. Our liability is limited to the fee you paid for that pack."* The oral *"a board tells you"* standard is gone from both specs. Correction SLA reconciled to **five business days** everywhere (m13). Byte-equality across surfaces is test-enforced (`specs/12` AC8). The guarantee is affordable **because** B2 narrowed the promise: a disclosed gap is not a contradiction. |
| **B6** | **fixed differently** — see §4, founder can override | `specs/05` invariant 2 (rewritten as a table) + AC7 + AC7b | **The review asked for `confidence != "high" OR status != "verified"`, which flags every medium value. The iteration brief asked for the opposite — medium-verified allowed, low or unverified flagged — and that is what is applied**, with two mitigations that carry the same risk by a narrower instrument: (1) a `confidence != high` value **renders its `note` wherever the date appears** — dashboard, "why this date?", alert email, PDF — and (2) in a **paid Entry Pack** anything below `high` still goes in the needs-human-check block, which is the ontology's own existing rule and `specs/08` AC2. AC7 is rewritten to match (TX plumbing: `needsHumanCheck = false`, `confidence = medium`, note present in all four renderings) and AC7b added. **The reason for not flagging medium:** medium is a third of the launch data (every TX plumbing cycle, several NC fees), and a flag that fires on a third of the product stops being read — which loses the flag's meaning for the `low` and `unverified` cases where it is the only defence. |
| **B7** | **fixed** | `specs/07` §Screens (map section rewritten), §Data model (status enum), §Validation (constants), AC2/3/3b/3c, edge cases, test plan · `specs/02` `/onboarding/states` · `UX.md` S09 · `LANDING_SPEC.md` V1 | The dashboard is the **tile grid** — 51 tiles, equal weight, `design-system.css` `.sr-map` — with **one status vocabulary in caps everywhere**: READY / AT RISK / LAPSED / NOT TRACKED, plus hollow-dashed *not in your footprint*, which is a **rendering, not a fifth status**, and carries no status word in its accessible name. **AT RISK = 90 days**, and it is not a copied constant: `AT_RISK_DAYS === ALERT_OFFSETS[0]` is asserted by a unit test, and AC3b runs one fixture through both `specs/07` and `specs/06` so the grid and the first alert gate cannot drift apart. A lint rule forbids the literals "amber", "red", "green" and "ok" as status names. |
| **B8** | **split — fixed on the non-identity documents, `IDENTITY.md` delegated** | `UX.md` S09 (top bar, not left rail), S17 (theme `board / paper / system`), S18/S19/§7 (paper for forwarded artefacts) · `LANDING_SPEC.md` header, §4 V1/V2/V3/V5, §4.1 (rewritten), §9 fonts and transfer budget, §2.1 wireframe | The arbitration landed **during** this iteration and is final (`../IDENTITY_ARBITRATION.md`), so the review's premise — *"the CSS, not `IDENTITY.md`, is the source of truth, and the arbitration is not in the repository"* — is superseded. Applied to the two documents in my scope: **`--sr-paper` replaced by `--sr-ground` + the `data-theme="paper"` alternate**; **Public Sans / IBM Plex Mono replaced by Barlow / Barlow Condensed / Overpass Mono** (font budget 70 → 90 KB, page transfer 220 → 240 KB); **the light-first marketing assumption replaced by board-first**, with the rule that **paper is what leaves the building** — print, the bid PDF, the shared readiness link, the technician card and every email; **the left rail replaced by the top bar** (`.sr-bar`, `.sr-rail` still resolving). **Not done, and not mine:** the `IDENTITY.md` §§6.1–6.3, 8.1, 8.3, 8.4, 10, 11 rewrite and the §6.3 contrast table, which is the Brand Director's file (constraint). |
| **B9** | **fixed differently** — D8 overridden, see §4 | `specs/06` §Flow (rewritten), §API, §Validation, AC9/AC10/AC11, §Errors, test plan | **The review's D8 made Vercel Pro a wave-2 prerequisite and kept the hourly drain. The brief asked for a design that is correct on Hobby's one-invocation-a-day, and that is what is specified** — which is strictly safer, because it removes the failure mode where the schedule is silently coerced and nobody notices. The local-morning window moves **into the data**: each recipient carries `timezone`, `digestHourLocal` and a computed **`nextSendAt`**; one daily cron at 12:00 UTC drains everyone due before the next run. Three details that are the actual engineering: **(1)** the claim is `next_send_at <= now() + DRAIN_INTERVAL`, not `<= now()`, because the obvious formulation defers every recipient west of the drain time *forever* — AC9 is the regression test and it silences the entire Pacific coast without it; **(2)** offsets are evaluated as `due_on - today <= offset` taking the **largest unsent offset**, so a skipped run *delays* an alert and never *deletes* one (AC10); **(3)** `DRAIN_INTERVAL` is derived from the cron expression at boot, so **going hourly on Pro is a one-line config change with no code change**, and a sub-daily expression on a Hobby project **fails the build** (AC11). `digestHourLocal` stays real rather than becoming display-only, and the limitation is stated on `/settings/notifications`, in the help article and in the digest footer. |
| **B10** | **fixed** | `specs/06` §The schedule, §Data model (rewritten), §Screens, §Validation, AC5, edge cases, test plan | `alerts` is now **per (deadline, offset, recipient)** with `unique(deadline_id, offset_days, recipient_user_id)`; `digests` is **per (recipient, local send date)** with `unique(recipient_user_id, send_date)`; a new `alert_recipients` table carries the scheduling state and is what the drain claims. AC5 is now testable at the row level: two recipients, one deadline → two alert rows and two digests, and a bounce on one leaves the other's delivery untouched. The anti-duplicate guarantee is explicitly per recipient — the organisation-wide version meant a two-person compliance team could only ever be told once, between them. |
| **B11** | **fixed** | **new `kb-scripts/accept_drift.py`** · **new `kb-scripts/test_accept_drift.py`** (17 assertions, passing) · `kb-scripts/validate.py` G10 · `kb-scripts/refresh_sources.py` (`--write-baseline`) · `specs/14` §Flow (rewritten), §Data model, invariants, AC6/6b/6c/6d · `ontology/id-grammar.md` | `no_change` now **has an effect**: one command re-fetches the source, writes the new hash **to the baseline and to every citing record's provenance together**, appends a `_history/{record_id}.jsonl` line, and re-runs `validate.py`, reverting everything if it does not exit 0. `resolveDriftItem` does not write files (the runtime never mutates the repo); the queue item shows **`no_change — awaiting acceptance`** with the exact command until a deploy lands whose baseline matches. **G10 is scoped to citation**: a hash mismatch fails the build only where a `SourcedValue` actually cites that page, and is a **warning** — named, visible, not build-breaking — where nothing does. The test proves the coupling in both directions: baseline-only rewrite of a **cited** source fails G10; of an **uncited** source it warns and deploys; a full acceptance leaves `validate.py` at exit 0 with **no value, status, confidence or `last_verified` touched**. |

### 2.2 Major

| id | status | files · sections changed | the decision taken |
|---|---|---|---|
| **M1** | **fixed** | `BACKLOG.md` §1 (**M16**, **M17** added) · `UX.md` §9 (four new rows), §10 self-review | **S15 qualifier watch → M16** and **S19 shared readiness link → M17**, exactly as recommended: the differentiator (`IDENTITY.md` §2 UA3) was modelled in `specs/05` as a `qualifier_replacement` deadline kind and rendered nowhere, and the distribution mechanism (J5) was self-certified as served by nothing. **S02, S13, S14, S18 stay SHOULD with named triggers**, and `UX.md` §9 now lists each one as *not being built* rather than leaving them stranded between two documents. |
| **M2** | **fixed** | `BACKLOG.md` §1 **M15**, effort total, §5 sub-wave C | **M15 — marketing route + no-login State Rulebook demo, ~3 dev-days, depends on M14.** Total restated: **~40 dev-days** (34 + M15 3 + M16 2 + M17 1). M15–M17 have no spec file yet and the backlog says so: writing them is the first job of sub-wave C. |
| **M3** | **fixed** | `UX.md` §1 · `BACKLOG.md` S8 · `OFFER.md` §7/§8 · `LANDING_SPEC.md` §0/§12.2 | D2: **the demo is the single free entry point.** Stated explicitly that the 14-day trial is not a second free entry point but what the account does for its first fortnight — the two are sequential, not alternatives. |
| **M4** | **fixed** | `specs/05` §Analytics · `specs/04` §Analytics · `specs/11` §Analytics · `specs/13` (unchanged, now correct) | **One name each.** `licence_deadline_derived` is emitted **from the derivation service in `specs/05`**, not from the create path — so import, profile change, KB publish and the nightly cron all count, which is the under-counting the review predicted. `why_this_date_opened` is deleted in favour of `deadline_explained`. `specs/13`'s parity test now means something. |
| **M5** | **fixed** | `specs/13` §Screens, AC3b | The copied band is deleted and replaced by a format string. **Every band is read from the generated `thresholds.json`**, and AC3b greps the whole codebase — tests and fixtures included — for the literal band values, failing on any hit outside the generated file and its generator. The band had already moved five points before a line of code existed. |
| **M6** | **fixed** | `specs/09` §Stripe list (rewritten as the canonical table) · `OFFER.md` §12 (now points at it) | **One list, in `specs/09`.** `STRIPE_PRICE_MULTISTATE_*` wins; the **$1,000 add-on** is added as line 10; the credit mechanism is stated as **open founder question Q8** in both places rather than settled in one and open in the other. `STRIPE_PRICE_FIRST_STATE_AUDIT` is struck with an instruction not to create it. |
| **M7** | **fixed** | `OFFER.md` §12 · `specs/09` §Stripe list note 3 · `specs/08` pricing | **One credit per customer, whichever is larger**, enforced in the app. Written now even though the audit is deferred and the stacking hole is moot at launch, because the rule is cheaper to write than to retrofit. |
| **M8** | **fixed** | `specs/09` "Above the 15-state cap" (new) + AC10 + `enterprise_enquiry_created` · `OFFER.md` §7 · `LANDING_SPEC.md` §5 · `BACKLOG.md` §0 (D4) | **Resolved with an explicit "contact us" row that the outbound playbook can route**, not an invented price. The row is published on `/pricing` and in the ladder, carries **one number we control** — *"a quote within two business days, or we tell you we cannot help"* — and is backed by `POST /enterprise-enquiry`, which writes the enquiry, emails the founder with the state and technician counts pre-filled, confirms to the customer, and emits an event. The 16th state hits the same route instead of a dead end. For wave 3: `state_count` column, sorted, first batches lead with accounts inside 15 states, accounts above 15 route to an `enterprise_quote` stage. **No price was invented** — we have no basis for one and a made-up rate rots the whole card. |
| **M9** | **fixed** | `KNOWLEDGE_BASE.md` §14 Q1 | The sentence is corrected to the counted figure — **5 of 20** in uncovered trades, 14–15 in covered ones — and Q1 is answered with **D5** and its three reasons (the count; roofing is state **and** county, a different data model; H7 already bets activation on covered-state count). GA/OH/AZ/MI named as the first tranche with the reason (one board each, ~14 agent-hours, and Georgia closes the Texas/NC reciprocity loop). |
| **M10** | **fixed** | `KNOWLEDGE_BASE.md` §5 (pass-B table rewritten with **proves / catches / cannot see** rows) + §5.3 | §5 now says what pass B **proves** — *the quoted fragment is still literally present at the cited URL* — and states plainly what it **cannot see**: whether the reading is correct. The `tx.hvac` $74 exam fee is written in as the worked example. The commercial consequence is named: **the Accuracy Guarantee is adjudicated on the reading, not on the fragment**, which is why medium-confidence values now render their note (B6) and why a customer-reported error is a top-of-queue drift item rather than a support ticket. |
| **M11** | **fixed differently** — with B6 | `specs/05` invariant 2 · `KNOWLEDGE_BASE.md` §5 | The review offered two routes: add a `status: "inferred"`, or make medium-verified behave like unverified everywhere. **The first is an ontology and data change I was constrained out of** (and would require editing `tx-plumbing`'s values); **the second is what B6's brief reversed.** What is applied instead is the rule that removes the dependency the finding actually identified: **no component may key on `status` alone** — `confidence` and `needsHumanCheck` are both carried on the deadline row and the render helper takes both — plus the note-everywhere rule and the fail-closed rule in m5. `status: "inferred"` is recorded as the right wave-2 ontology change. |
| **M12** | **fixed** | `specs/12` §The disclaimer + AC7 | The cadence claim is **out of the legal page**. It becomes a **target on `/help/methodology`**, printed beside the live figures that show whether we are meeting it. The legal page carries only what is structurally true — the provenance line on every value, and the 180-day rule enforced in code. AC7 greps `/legal/disclaimer` for "daily", "monthly", "every month" and **fails on a match**. |
| **M13** | **split — engine rule fixed, schema + gate delegated** | `specs/05` new subsection "Board-announced date rolls — `expiry_overrides`" | The engine rule is specified in full: an override for the derived cycle year **wins over the token**, carries its own citation, is shown in the "why this date?" trace, and is a `SourcedValue`-shaped object going through the same two-pass verification. The Florida 2 September extension is quoted as the worked example. **The `ontology/schema.state_trade_record.json` addition and the G8 extension are not done** — `ontology/` was constrained to the `sourced_value` tightening this iteration — and the spec names the exact patch required, flags that `additionalProperties: false` makes the field unrepresentable until it lands, and calls it **the first schema change wave 2 should make**. |
| **M14** | **fixed** | `UX.md` S02 (four conditions) · `BACKLOG.md` S8 | Deferred by D2, which removes the exposure at launch. The four conditions for whenever it is built are written into the screen spec as requirements, not suggestions: **parse in the browser**, never persist, **privacy notice above the paste box**, `/legal/privacy` names the flow in its own paragraph, and the exception list computed client-side — *"if that proves impossible, the feature does not ship; it does not quietly become a server upload."* |
| **M15** | **fixed** | `specs/08` §Test plan | The founder review moves **behind** delivery: deliver immediately, read within 24 hours, and **trigger the guarantee proactively** if the read finds an error — rewrite, refund, and email the buyer before they have opened it. The purchase screen says so in one line. A buyer who pays $750 at 22:00 on a Friday gets the document at 22:01. |
| **M16** | **fixed** | `kb-scripts/refresh_sources.py` (`EXCERPT_CHARS`, `--write-baseline`) · `kb-scripts/accept_drift.py` · `specs/14` §Data model (`baselineHead` / `baselineTail`) | Bounded 4,000-character head and tail of the normalised text stored with the hash — ~280 KB across all 35 sources — written at baseline time and by `accept_drift.py`, never by the daily check. `specs/14` states the honest limit: where a change falls outside both windows the screen **says so and links the live page** rather than showing a diff it cannot compute. Not run against the live baseline (that would re-fetch 35 board pages and move hashes mid-review); the test covers the code path with a local file. |
| **M17** | **fixed** | `kb-scripts/accept_drift.py` · `kb-scripts/test_accept_drift.py` · `ontology/id-grammar.md` §Versioning | `_history/{record_id}.jsonl` is **implemented** rather than deleted: one JSONL line per record per acceptance, carrying the previous hash, the new hash, the date, the resolver, the note and the json paths of the citing values. The ontology paragraph now names what writes it and what asserts it. The test checks the line exists, has all five fields, and that the acceptance touched no value. |
| **M18** | **fixed** | `LANDING_SPEC.md` §4 + §13 | The California and NYC quotes stay — they are the best-sourced consequences we have and they are about the **category** — and a **Texas** line is added between them: *"You may not engage in air conditioning and refrigeration contracting if your license has expired."*, verified in `kb-data/tx-hvac.json` (`licence_types[0].renewal.grace_period`, `verified`/`high`, `tdlr.texas.gov/acr/contractor-renew.htm`). The closing line becomes *"Three states. One rule…"*, which does the coverage framing implicitly. §4 goes 76 → 97 words; the section is re-counted and so is the deck. |
| **M19** | **fixed** | `LANDING_SPEC.md` §12.1 (wireframe rewritten), §12.2, §12.3 | **The bond row is out of the demo's default view** and is replaced by a **"What Texas does not publish"** panel naming the unverified fields, saying how many pages we read looking, and linking them. The four rows above it are the ones verified in every launch record. The refusal becomes the proof point instead of an accident, and the definition of done asserts the default view contains **no unverified value row** — against the committed record, so filling the bond field later updates the demo and breaks nothing. |

### 2.3 Minor

| id | status | change |
|---|---|---|
| m1 | **fixed** | `BACKLOG.md` §0: **15–100** technicians, reconciled with `PERSONA.md` §1 and `OFFER.md` §1, with the reason (below ~10 licensed people a spreadsheet works and we say so). |
| m2 | **fixed** | `specs/08` pricing cross-reference now cites `OFFER.md` **§6.3** (Entry Pack pricing) and **§7** (the ladder). |
| m3 | **declined** | The schema `$id`s still say `https://stateready.app/…`. Two reasons: `ontology/` was constrained this iteration to the `sourced_value` tightening; and the right value depends on **P11**, a founder gate — the name is provisional, `stateready.app` is NXDOMAIN, and the USPTO knock-out has not run. Picking a URI now risks a second rename. **For wave 2:** use a repo-relative `$id` unless and until the founder registers a domain. |
| m4 | **fixed** | `ontology/schema.sourced_value.json`: `source_url` / `evidence` / `last_verified` / `verified_by` are now required on **every value that is not null**, not only on verified ones; `verified_by.minItems: 2` under the verified branch, mirroring gate G1 so the schema is not weaker than the gate; a new branch requires `note` whenever status is `unverified` or `unknown`. **Proved by experiment**, not assumed: the real records still validate at 0 failures, and a deliberately demoted value with its URL stripped now produces three schema errors where it previously produced none. Written as a positive type list rather than `not: {type: null}`, because `validate.py`'s schema subset does not implement `not` and would have **silently skipped the branch** — the exact failure the file warns about. |
| m5 | **fixed differently** | Extending gate G3 is a `kb-scripts` change outside this iteration's edit constraint (drift fix only). The customer-facing half is closed in the renderer instead, and it fails **closed**: `specs/05` invariant 2 now sets `needsHumanCheck = true` for any `confidence != high` value **with no note** — so `tx-plumbing` licence types [1] and [2] (`renewal.cycle = 12`, medium, `note: null`, verified in the data) flag rather than showing a confident date with nothing behind it. AC7b asserts it against the committed records, so the test starts passing on its own the day someone writes those two notes. The G3 extension is recorded in `specs/05` as a wave-2 task. |
| m6 | **delegated** | Sentence-case status words, the undocumented fifth state and the duplicated accessible names live in `identity/samples.html` and `identity/build-samples.py`; regenerating the samples file is out of scope (Brand Director). `specs/07` now **documents** the fifth rendering — hollow-dashed *not in your footprint* is **not a status** and carries no status word — and mandates the four-word all-caps vocabulary everywhere, so the identity fix has a specification to conform to. |
| m7 | **fixed** | `UX.md` E16 becomes **three rows** — day 7, day 12, day 14 read-only — adopting `specs/09`'s cadence, with the reason for 12 rather than 11 written down. |
| m8 | **fixed** | `UX.md` E14: the renewal-week brief is **on by default on paid plans, off during the trial**. A bonus nobody receives is not a bonus. |
| m9 | **fixed** | `LANDING_SPEC.md` §1 table now lists **both CTA repeats** as counted rows, and the deck marks all three placements. Deck, table and CI now report the same number: **413**. |
| m10 | **declined (recorded)** | `offer/CLAUDE.md` is the Offer agent's memory and is outside my edit list. The authoritative count lives in `LANDING_SPEC.md` §1 with the CI rule beside it and is **413**; §1 also carries the section-by-section derivation from the old 398, so the memory's stale 386 is superseded by a document that shows its working. |
| m11 | **fixed** | `ontology/id-grammar.md`: the source-id example is now the real `tx.tdlr.acr_apply` (verified present in `_sources.json`). A one-word factual correction; it changes no schema semantics. |
| m12 | **fixed** | `specs/12` refund policy now names all three positions explicitly — the two that ship, and the **Alert Guarantee as not in force and on no surface until counsel**, with AC8 failing the build on its text. |
| m13 | **fixed** | Correction SLA is **five business days** in `OFFER.md` §5.1.1, `specs/08` and `specs/12`. One business day is a single-founder SLA with no cover behind it. |
| m14 | **fixed** | `specs/02` `/onboarding/states` uses the **tile grid**, the same component as `specs/07`. |
| m15 | **fixed** | **180 days** → the value stops rendering as verified, badge and `needsHumanCheck` and the board link, specified in `specs/14` invariant 2 and answered in `KNOWLEDGE_BASE.md` §14 Q3. A **runtime rendering rule**; G13's 400-day build-breaking backstop is untouched, so no `kb-scripts` change was needed. |
| m16 | **fixed** | No data change. `specs/08` item 5 and `LANDING_SPEC.md` §12.2 both require a medium-confidence value to render **its note, not just its number**; `specs/05` invariant 2 makes it a product-wide rule and m5 makes it fail closed. |

### 2.4 The review's other checks

| item | state |
|---|---|
| **Q10** — the "no job stops" headline | **Deleted from the A/B test plan** (`LANDING_SPEC.md` §2.2, §10). It is an outcome promise the guarantee section refuses to stand behind and `IDENTITY.md` §2 prohibits it. Returns only on a written founder acceptance; §10 now adds the general rule that **a headline, a guarantee line or a coverage claim is not an A/B variable**. |
| **Q12** — EPA 608 $44,539/day | Appears on **no surface**. Re-grepped: the only occurrences are the ban records themselves (`PERSONA.md` §3 "UNVERIFIED — DO NOT PRINT", `IDENTITY.md` §11 never-list, `identity/sources.md` row 22). Added to `LANDING_SPEC.md` §11's permanent prohibitions with "re-grep before every deploy". |
| **Q13** — Illinois plumber CE hours | Appears on no surface; only in the research files recording why it may not be used. Added to `LANDING_SPEC.md` §4 and §11: **use the 30 April date, never the hours**. |
| **Q7 / Q8 / Q9 / Q11 / Q15 / Q18** | Defaults applied: 180 days (m15); customer balance credit, one per customer, larger of the two (M6/M7); Enterprise quote-only **with a routable "contact us" row** (M8); **say nothing about capacity** — `OFFER.md` §9 now settles this rather than leaving it pending, since D1 removes the only capacity-limited thing in the offer; Alert Guarantee held for counsel (B4); the spike written as S10 with acceptance criteria (B3). |

---

## 3. Evidence, re-run at the end of the iteration

| check | command | result |
|---|---|---|
| KB schema + 13 gates | `python3 kb-scripts/validate.py` | **exit 0** — 9 records, 0 failures, 3 warnings (the same three correct G7 warnings on Florida reciprocity) |
| The schema tightening actually fires | demoted one verified value in a temp tree, stripped its URL | **3 schema errors** where the wave-1 schema produced none |
| Drift acceptance | `python3 kb-scripts/test_accept_drift.py` | **17/17 assertions pass** — both hashes move together, half an acceptance fails G10 on a cited source, an uncited source warns instead of failing, `_history` is written, `--dry-run` writes nothing, and **no value, status, confidence or `last_verified` is touched** |
| Python syntax, all scripts | `ast.parse` over `kb-scripts/*.py` | clean |
| Landing word budget | mechanical re-count of §13 under §1's own rule | **413 words** above pricing, ceiling 450, **37 headroom**, all three CTA placements counted |
| Event-name parity | `grep` over `specs/*.md` | one activation event name (`licence_deadline_derived`), one panel event (`deadline_explained`); the remaining occurrences of the old names are the sentences recording the rename |
| Stale-token sweep | `grep` for `--sr-paper`, `Public Sans`, `IBM Plex`, `light / dark / system`, `amber ≤ 60`, `STRIPE_PRICE_MULTI_`, `"tells you"` | zero live occurrences in the documents in scope; the surviving hits are the sentences that record the correction |

**Not re-run, deliberately:** `refresh_sources.py` and `verify_pass_b.py` hit 35 live board pages over
several minutes at 1.5 s spacing. Nothing in this iteration touched a record's values or its evidence
fragments — the drift test proves that at the row level — so their last results stand. Wave 2 should
run both before its first commit, per `KNOWLEDGE_BASE.md` §13.

---

## 4. Founder can override

Four calls made without a founder, each taking the option that reduces liability and keeps the product
free of a human loop. Each names the lever that reverses it.

| # | the call | why it was made this way | how to reverse it |
|---|---|---|---|
| **1** | **D1 — free trial rather than the $149 tripwire**, and the roster build deferred | The tripwire owes a deliverable only a human can currently produce, and it collapses T2 by construction. Deferring costs 14 days of cash per signup; shipping it would have owed every buyer a hand-built roster from day one | Run **S10** (2 dev-days). If a majority of the fifteen states are automatable, reverse D1: `THRESHOLDS.md` §7 first, §2 second, then the CTA and the Stripe price. It is one config plus one CTA |
| **2** | **B6 — a medium-confidence verified value is allowed through unflagged**, carrying its note, and flagged only in the paid pack (and flagged in the product when it has **no** note) | The iteration brief specified it; the reviewer recommended the opposite. Flagging medium would put roughly a third of the launch data behind a warning — every Texas plumbing cycle — and a flag that fires on a third of the product stops being read, which loses it for the `low` and `unverified` cases where it is the only defence | One line in `specs/05` invariant 2 (`confidence != "high" OR status != "verified"`) and AC7 back to the wave-1 wording. **If the founder wants the stricter reading, take it before wave 2 builds M5**, not after — it is a one-line change now and a re-test of ~60 golden cases later |
| **3** | **B2 — the Entry Pack promise is narrowed rather than the sale being blocked** | Blocking on bond, fees and timeline would make **zero of the nine records purchasable** and would only be liftable by human research. Narrowing keeps the one revenue line that works on day one, and the guarantee is affordable precisely because a disclosed gap is not a contradiction | Verify the fields. The gate already exists (`entryPackReady`): moving `bond.amount` and `typical_timeline` from the DISCLOSED_SET to the CORE_SET in `specs/08` is a two-line change once the data supports it |
| **4** | **B9 — a daily cron designed to be correct, rather than an hourly cron that needs Pro** | An alerting product whose schedule is silently coerced is worse than no alerting product, and P1 was still `todo`. The design is correct on one invocation a day and becomes exact on Pro with a config change | Buy Pro and change the cron expression to `0 * * * *`. `DRAIN_INTERVAL` follows it automatically; no code changes |

**And one thing that is not mine to reverse:** the Alert Guarantee (B4) is drafted, capped, carved out
and **not shipped**. Publishing it is a founder decision that should follow counsel (Q15), and
`specs/12` AC8 currently fails the build if its text reaches a page.

---

## 5. What the build fleet should read first

1. **`specs/05`** — invariant 2's table is the honesty rule for the whole product, and D6 says build this
   module first, as a pure function with golden tests over `kb-data/`, before any schema exists.
2. **`specs/06` §Flow** — the three details that make a daily cron correct, and the two regression tests
   that fail without them.
3. **`specs/08`'s `entryPackReady`** — and the sentence that goes with it: the pack promises what the
   board publishes and names what it does not.
4. **`kb-scripts/accept_drift.py` and its test** — the drift queue is abandoned in week one without it.
5. **`REVIEW.md` §5.1** — still accurate about what wave 2 can start today, with M15/M16/M17 added.

---

*Written against `PIPELINE.md` stage 6. `REVIEW.md` was not edited; neither were `IDENTITY.md`,
`design-system.css`, `identity/samples.html`, `identity/contrast.py`, or any value in `kb-data/`.
No commits, no pushes.*

---
---

# Round 2 — closing the re-review

**Author:** Iteration agent (StateReady), phase-4 fleet, closing author for wave 1b. **Date:** 2026-09-03.
**Answers:** the **Re-review** section at the top of `REVIEW.md` — **1 blocking · 3 major · 4 minor open**
(R1, N1, N2, M13's schema half, m6, N3, N4, N5).
**`REVIEW.md` was not edited.** Neither was `design-system.css`, nor any value in `kb-data/` — the only
write into `kb-data/` this round is the excerpt backfill in `_sources.json`, which is what N1 asked for
and which touches two keys per source and nothing else (proved below).

> ## Outcome — **all eight closed.**
>
> | id | severity | closed how |
> |---|---|---|
> | **R1** | blocking (regression) | Entry Pack Guarantee **verbatim on the landing page**, Accuracy Guarantee compressed under three tested conditions; all three documents rewritten to say the same thing |
> | **N1** | major | `refresh_sources.py --fill-excerpts`, a 14-assertion test, and **35 of 35 sources now carry a "before"** |
> | **N2** | major | Eight unbacked features out of `OFFER.md` §7's Contains column, plus the two of the same class in §3 and §4 |
> | **M13** | major (half) | `expiry_overrides` in the ontology, **G8 extended**; `validate.py` exit 0 |
> | **m6** | minor | `identity/samples.html` regenerated from its generator; the contradiction with `specs/07` is gone |
> | **N3** | minor | one token |
> | **N4** | minor | `specs/04`'s Requirements panel in B2's wording, with an acceptance criterion behind it |
> | **N5** | minor | AC8's count, and the `§5.1.3` citation class killed at its source |
>
> **Evidence, re-run after the last edit:** `validate.py` **exit 0** (9 records, 0 failures, the same
> 3 G7 warnings) · `test_accept_drift.py` **17/17** · `test_fill_excerpts.py` **14/14** ·
> `identity/contrast.py` **exit 0** (4.89:1 text, 3.15:1 non-text) · `../scripts/identity-distinctness.py`
> **exit 0** · landing copy deck **439 words** of 450, counted mechanically under §1's own rule.
>
> **Two paragraphs are now stale in files this round was not allowed to edit** (§6.3). Neither is a
> promise to a customer; both are one-line corrections for the agent who owns the file.

---

## 6.1 The eight items, one row each

| id | status | files · sections changed | the decision taken |
|---|---|---|---|
| **R1** | **fixed** | `OFFER.md` §5.1 (regime table + numbering note) · `specs/12` refund policy + **AC8 rewritten as a–d** + test plan · `LANDING_SPEC.md` header, §1 (budget), §7 item 3, **§8 rewritten**, §13 deck, §13.1 rules | **The landing page carries the Entry Pack Guarantee verbatim — all 63 words — and compresses only the Accuracy Guarantee.** The reviewer offered the opposite fix (exempt the marketing route on conditions) and the exposure argument in the finding is the reason for not taking it: the compression *"Entry Pack contradicted by the board's own page? We rewrite it and refund you"* drops the **90-day claim window** and the **cap at the fee paid**, so it reads as a **larger** promise than the one we wrote, on the only page a stranger reads before paying. A compression that removes the limits and keeps the promise is the wrong direction of error. The Accuracy Guarantee, whose liability is one month's credit, stays compressed under four conditions that AC8c **tests**: it contains `five business days` and `one credit`; it links to `/legal/refunds`; it adds no quantity and no escalation word (a verb may be shortened, a number may not move); and it never says "guarantee" in a strip with no link. **The loose version of that third condition — "no word the full text does not use" — fails against its own approved copy**, which is the same class of error as R1 itself, so AC8c states it mechanically. **The rule is now stated once, in `OFFER.md` §5.1, as a table of surface × form**, and the other two documents point at it rather than restating it — which is how the three drifted apart in the first place. |
| **R1 (budget)** | **paid for** | `LANDING_SPEC.md` §1 | The verbatim block costs **+52 words**. Three cuts pay for it, all of them prose that repeated something the page already showed: §5's *"Every answer shows where it came from and when we checked"* (**−11**, the demo renders a source chip on every answer), §7's *"Every date shows its source and the day we checked it"* (**−11**, the third statement of one claim), §4's *"Three states. One rule."* (**−4**, rhythm). **413 → 439 of 450**, headroom 37 → **11**, and §1 names the next two cuts so the next editor does not have to invent one. Strip §8 and the CTA repeats out of both columns and the page's *arguing* copy went **355 → 335**. |
| **N1** | **fixed, and run** | `kb-scripts/refresh_sources.py` (new `--fill-excerpts`, `--dry-run`, `--from-dir`) · **new `kb-scripts/test_fill_excerpts.py`** (14 assertions) · `kb-data/_sources.json` (excerpts only) | `--fill-excerpts` re-fetches, and **writes `normalised_head`/`normalised_tail` only where the re-fetched `content_sha256` equals the stored one**. A source that has moved is named in the report, told to go through `accept_drift.py`, and **left byte-for-byte alone**; an unreachable one likewise, because "we could not read it" is not "it is unchanged"; and the mode exits non-zero if it refused anything, so CI cannot pass on a half-filled store. **Run once against all 35 sources: 35 filled, 0 drifted, 0 unreachable** — every launch source still matches the hash it was authored against. A key-by-key diff of the committed baseline confirms **no key other than the two excerpts changed on any source**, and no record was touched. The test's load-bearing assertion is the refusal: a drifted source's entry must be *identical* afterwards, not merely un-excerpted. |
| **N2** | **fixed, and widened by two** | `OFFER.md` §7 (four rows rewritten + the rule + a removal table) · §3 stack item 5 · §4 bonus table | **The rule is written into the document: a feature may appear in the Contains column only if it has a Must or a Should with a number.** The eight came out — *rule-change watch*, *subcontractor credential tracking*, *audit log* and *acquisition intake checklist* have no backlog item at all; *bond & insurance certificate tracking* (**L5**), *multi-entity / per-brand separation* (**L6**) and *webhooks* (**L4**) are LATER; and the free row's *bond/insurance minimums* is the claim **B2** removed everywhere else. What is left carries its id (**M6**, **M7**, **M8**, **M14**+**M5**, **M16**), and the one Should is labelled *planned* with it (CE-provider directory, **S11**). **Two more of the same class were in the document and not in the finding:** §3's stack item 5 sold the rule-change watch as a core component, and §4 sold the acquisition intake checklist as a Platform-tier bonus. Both are struck with the reason, in the idiom §3 already uses for the deferred roster build. Platform's row is thinner and true, which matters most at $599, where the buyer is the one person in this market who will check. |
| **M13** *(schema half)* | **fixed** | `ontology/schema.state_trade_record.json` (`expiry_overrides` on `licence_types[]`) · `kb-scripts/validate.py` **G8** | The field is now representable: an array of `{cycle_year, date, source_url, evidence, last_verified, verified_by}` with optional `confidence`/`note`, `additionalProperties: false`, `verified_by` `minItems: 2`. **G8 grew from one assertion to six**, because an override is the one thing in a record that can move a deadline *without* a `SourcedValue` wrapper, so the checks G1/G3/G4/G5 do for values are done here for overrides: the date is a real date **inside its `cycle_year`**, one override per cycle, two distinct verifiers, evidence ≤ 25 words, `last_verified` not in the future, and the source host on the `official-hosts.json` allowlist — plus a refusal to attach an override to a licence type whose `expiry_rule` the engine does not implement. Verified in a throw-away tree: the Florida 2 September 2027 override from `specs/05` validates (exit 0), and `date` outside the cycle year, an impossible date, a duplicate cycle, one verifier, an off-allowlist host and an unknown property each fail. `kb-data/` is unchanged — **no record carries an override yet**, and wave 2 can now write one. |
| **m6** | **fixed** | `identity/build-samples.py` (the `WORD` dict, a new `tile_label()`, the legend, the table, the chips, the status-text spans) · `identity/samples.html` **regenerated** · `specs/07` (the paragraph that recorded the contradiction) | The generator reproduced the committed file byte-for-byte before the change, so the diff is exactly the fix: **the four status words are the all-caps names** (`READY / AT RISK / LAPSED / NOT TRACKED`) in the legend, the table, the chips and the inline status text; and **the hollow tile carries no status word**, with the accessible name `specs/07` prints — *"CA — not in your footprint"* — instead of *"CA — Not operating. Not operating"*. `WORD` deliberately **has no entry for the hollow case**, so the fifth-status mistake cannot be made again one tile at a time. No token, no colour, no CSS: `contrast.py` and the distinctness gate both still exit 0. |
| **N3** | **fixed** | `IDENTITY.md` §7.1 | `--sr-paper` → `--sr-ground`. It was the last undeclared token cited anywhere; the only surviving occurrences in the three documents are the four sentences that *record* the rename. |
| **N4** | **fixed** | `specs/04` §Screens + a new Requirements-panel section + **AC8** + test plan | The panel is specified in **B2's wording**: it renders every requirement the board publishes, each with its chip, and **names every one it does not** — *"the board does not publish this"*, with the note recording what we read looking for it. Bond and insurance are called out explicitly, because `bond.amount` is `unknown` **23 times out of 23** in the committed records, and the wave-1 wording promised a citation the data cannot supply. **AC8 makes it testable** over the committed `kb-data/`: every DISCLOSED_SET field with status `unknown` renders the field name and the not-published wording, and no such row renders a source chip. A field with no board answer is a **rendered row, not a missing one**. |
| **N5** | **fixed** | `specs/12` refund policy + AC8 · `OFFER.md` §5.1 | AC8 no longer says "three wordings" and then asserts two: it asserts **exactly two canonical wordings site-wide** (AC8a, AC8d), and the withdrawn third fails on sight. The `OFFER.md` **§5.1.3** citation is replaced by **§5.1 item 2** — and the class is killed at its source: §5.1 now states its own numbering (*§5.1.1 = item 1, §5.1.2 = item 2, there is no §5.1.3 — that was the Rollout Guarantee's wave-1 slot*), so the remaining citation in `specs/08` resolves to a sentence that explains it rather than to nothing. |

## 6.2 Evidence, re-run at the end of round 2

| check | result |
|---|---|
| `python3 kb-scripts/validate.py` | **exit 0** — 9 records, **0 failures**, 3 warnings (the same three correct G7 Florida warnings), with G8 now six assertions wide |
| `python3 kb-scripts/test_accept_drift.py` | **17/17 assertions pass** — unchanged by this round, re-run after the `refresh_sources.py` edit because the test copies that file into its tree |
| `python3 kb-scripts/test_fill_excerpts.py` | **14/14 assertions pass** — including *"DRIFTED source: entry is byte-identical to before"* and *"no key other than the two excerpts changed on any source"* |
| `python3 kb-scripts/refresh_sources.py --fill-excerpts` | **exit 0** — filled 35, refused 0, unreachable 0. `kb-data/_sources.json` now carries a "before" for **35 of 35** sources |
| `python3 identity/contrast.py` | **exit 0** — 70 pairs, smallest text margin 4.89:1, smallest non-text 3.15:1, 0 failures |
| `python3 ../scripts/identity-distinctness.py` | **exit 0** — 3 apps, 3 ground pairs, 3 typeface pairs |
| Landing word budget, re-counted mechanically | **439** of 450 — 68 / 37 / 93 / 20 / 66 / 51 / 9 / 86 / 9, and §1's table, §13's per-section figures and the deck's own strings all say the same number |
| `expiry_overrides`, tested rather than trusted | The `specs/05` Florida override validates in a temp tree (exit 0); six malformed shapes each fail with a named G8 message. `kb-data/` untouched |

## 6.3 Two paragraphs this round could not reach

Both are inside this iteration's stated edit scope for *other* people's files, and both are now factually
behind the code. Neither is customer-facing.

1. **`specs/05`, "Schema and gate work this requires, and which this iteration did not do".** It says
   `expiry_overrides` is unrepresentable and names the patch. **The patch has landed** (M13 above), so
   that paragraph should become a one-line "done, see `ontology/schema.state_trade_record.json` and G8".
   `specs/05` was not in this round's edit list; the ontology field's own `description` names `specs/05`,
   so the link is discoverable from the schema side.
2. **`specs/08` §Guarantee** still cites `OFFER.md` **§5.1.3** and asserts byte equality "in `OFFER.md`
   §5.1.3, `specs/12`, the purchase screen…". The citation now resolves to §5.1's numbering note, which
   explains that the slot is the withdrawn Rollout Guarantee, so nobody is misled — but the tidy fix is
   two characters, and `specs/08` was not in scope either.

*Round 2 of 3. `REVIEW.md`, `design-system.css` and every value in `kb-data/` are unedited; no commits,
no pushes. The closing note above §6 describes round 1 — round 2 did edit `IDENTITY.md` (one token) and
`identity/samples.html` (content only, regenerated from its generator), both explicitly delegated to this
round by the re-review.*
